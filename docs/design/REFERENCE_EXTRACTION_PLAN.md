# Reference extraction & faction mapping — the plan

**Owner: Claude-Local.** Lane: `tools/reference/**`,
`tools/balance/{assign_references,faction_routes,faction_extrapolate}.py`, `docs/balance/review/**`,
`docs/reference/**`. No other agent should edit these while this runs.

This is the build order for turning the reference corpus into per-class targets. It supersedes
nothing — it *implements* `REFERENCE_METHOD.md` and the maintainer rulings of 2026-09-05 recorded
in §0 below.

---

## §0 — Maintainer rulings, 2026-09-05. Do not re-litigate these.

### September 9 follow-up rulings

Aedis's direct instructions for the Japan pilot update the execution of the earlier
rules, not their raw-source or one-roster/one-vote safeguards:

- Initial reference placement includes **stats and price**. Classic original prices
  may change; subsequent fitting may adjust stats and price jointly on the existing
  permitted grids. A near-zero formula delta is not gameplay validation.
- Implement source-relative versus fitted-target separation; freeze pilot calibration
  inputs; expose counterpart/analogue/extrapolation status and disagreement; validate
  withheld references; and compare representative counter/tier/TTK relationships before
  recommending live changes. These are distinct checks, not one numeric acceptance score.
- MCVs and harvesters are collection-only/manual-review subjects. Harvesters need their
  separate formula. Neither may silently enter automatic combat-unit fitting.
- Extract RA3, Dune II and Emperor reference evidence; obtain versioned Spice Wars data.
  Exact DTA re-extraction awaits the files Aedis will provide. Unsupported damage channels
  and unresolved source dependencies must remain visible and must not become complete
  numeric evidence merely because a column is present.

Instruction timestamps, implementation boundaries and pending evidence are recorded in
[the September 9 ruling record](../balance/review/AEDIS_OVERNIGHT_RULINGS_20260909.md).
These instructions do not sign off anchors or authorize merging PRs.

### Earlier rulings

| # | ruling |
|---|---|
| R1 | **Class anchors become VIRTUAL.** No class is anchored to a real actor any more. An anchor is a pure number at the **100% point of the price band** (the band is `FORMULA_V2.md` §3's 50–250% envelope of `C₀`). |
| R2 | **The verifier goes virtual too**, and is replaced by a stronger check: a new audit resolves **every** unit in a class through `miniyaml` and asserts the formula reproduces its price. This is strictly stronger than the single 2.5×`C₀` tripwire unit it replaces. |
| R3 | **Faction mapping is a unique SET, members may repeat.** No two Cameo factions share the same combination, but one reference faction may appear in several combinations. The existing law that a reference *unit* is spent once is unchanged. |
| R4 | **Equal thirds, and current Cameo yaml is always one of the voices** — `Cameo TD GDI = DTA GDI × Combined Arms GDI × current Cameo TD GDI`, each 1/3, synthesised by geometric mean. |
| R5 | **Faction profiles are computed per TYPE and overall** — infantry, vehicle, aircraft, naval, defense, and everything-combined — each compared against **the same group in the faction's own source game**, and each reported as an **independent separate value**. Geometric mean. |
| R6 | **Collect every stat we can get.** Some will turn out to be junk; that is a later triage, not a reason to narrow the extraction now. |
| R7 | **Variance is reported three ways**: coefficient of variation, min/max spread, and percentile position of the faction's mean within its own game. |
| R8 | ⚠ **AMENDED AT THE ENDS BY R19 (2026-09-21): beyond the outermost anchor the value is now EXTRAPOLATED, not held flat**, and both interpolation and extrapolation are geometric. The rest of R8 stands. **Armor extrapolation: interpolate between the peer's OWN declared rungs.** Every value a peer actually declares is a fixed anchor on its ladder; rungs between two anchors are interpolated; ~~beyond the outermost anchor the value is held flat~~ (see R19). A peer that declares only ONE rung on a ladder votes flat and is **downgraded to low confidence**. ⛔ Never distribute using Cameo's own §12.0i curve — that would make the "independent" reference partly a measurement of ourselves. |
| R9 | **Derived corpus is committed under `docs/reference/`; raw game data stays outside the repo** in `Cameo-mod-reference/`. |
| R10 | **Sequencing: the C&C family is built FIRST** — TD, RA1, TS, RA2 and the RA2-modded factions, from the references we have. Warcraft, StarCraft and Dune factions come later, each from **their own** reference pool (StarCraft mods, WC2 mods, Emperor: Battle for Dune, Spice Wars, the custom D2k mods). Measured and safe: of the **22 classes in use, ZERO are deferred-only** — every class a Warcraft/StarCraft/Dune unit sits in also has C&C members, and C&C members dominate all 16 mixed classes (`mbt` 33:8, `support` 30:4, `fire_support` 26:4). Deferred factions keep global-formula pricing meanwhile; waiting does not make them more wrong. ⛔ **The protection this depends on: anchor values are FROZEN design numbers. Re-fitting an anchor is an explicit maintainer act and must NEVER happen as a side effect of adding a reference source** — a re-fit moves every already-balanced faction underneath itself. ⚠ `archer` (2 C&C members) and `heavy_infantry` (1) are too thin to anchor on C&C evidence alone; leave them formula-only until their own references land. |
| R11 | **Cameo 1.0 release gate: EVERY faction must be written with the new balance formula, and EVERY faction must have reference data to choose from.** No faction ships 1.0 on formula-only pricing. The per-reference-faction calculated values are what populate each unit class relative to the baseline — so a faction with no reference has nothing to position against. This makes the deferred reference sources (Emperor, Spice Wars, StarCraft, WC2, the custom D2k mods) **release-blocking for 1.0**, not optional enrichment. |
| R12 | **DTA Enhanced represents DTA; Classic is a superseded lineage member.** They are one roster — Enhanced is Classic plus `Enhance.ini` — and measured 80–92% identical on the 863 shared ids with a median ratio of exactly 1.000. Enhanced is elected on the game's own default (`Items=Classic,Enhanced`, `DefaultIndex=1`). ⚠ Not a no-op: `td_gdi`, `td_nod`, `ra1_allies` and `ra1_soviets` move onto the overlay's ~10–20% rebalanced rows. |
| R13 | **Rise of the East is exclusive-only**, like Mental Omega and CnC Reloaded. Its 36% `asianalliance`/`tkm` overlap was inside the honest C&C band, so this is a cheap improvement rather than a rot fix: RotE has large per-country pools, so exclusivity costs ~4 rows a faction and takes the overlap to **0%**. |
| R14 | **The universal-pool carve-out.** A unit owned by EVERY routed country of its source is either shared infrastructure or a universal COMBAT unit; the mobile ones are readmitted, the structures are not. Found by asking what R13 actually removed: not harvesters — Rise of the East's Soviet Mammoth Tank, Asian Emperor Overlord Tank, Allied Juggernaut and Yuri Specter Squad, i.e. the top of the HP and cost range. ⚠ "Universal" means ALL routed countries; owned by SOME is the case the partition exists for and stays removed (Mental Omega's 61-unit non-Foehn pool; CnC Reloaded's Core Defender, 19 of 21 countries but not Nod). Overlap among the five CnCR-fed factions is 16–23%, against 81% before any exclusivity. |
| R15 | **A source's own NAMING beats a permissive `Owner=`, and a name claim is EXCLUSIVE.** CnC Reloaded ships a complete CABAL faction — 55 units named `CABAL's ...` plus the Core Defender pair, every one owned by `RobotCountry` — but gives 43 of them to all 21 countries, so ownership alone would discard the faction. A modder writes "CABAL's Refinery" on purpose and copies an `Owner=` line out of habit. A claimed unit goes to its claimant and to **nobody else**: CABAL's Cyborg Commando stops being a reference for `ra2_allies` and `ts_gdi`. `cabal` is routed to CnCR RobotCountry and goes **44 → 156 rows**, clearing the two-source floor it was the only routed faction to fail. ⛔ I had recorded that CnCR shipped no CABAL faction; that came from sampling DEVOUT and ASCENDED, seeing all-21 ownership, and generalising from two units. |
| R16 | **A cell an attack CANNOT REACH is `n/a`, never 100.** `WeaponInfo.ValidTargets` and `Warhead.ValidTargets` both default to `new("Ground","Water")` (WeaponInfo.cs:116, Warhead.cs:30), so a weapon that never mentions targeting cannot hit air at all — **366 of Combined Arms' 695 weapons**. Filling the unstated Aircraft row with the engine's 100 entered every tank cannon into the corpus as a competent anti-air weapon and polluted the whole AIR ladder: **74% of CA's air cells and 48% of Cameo's own**. `n/a` and "immune" (`Versus 0`) are both excluded from every mean and are **different statements**. |
| R17 | **Each source matrix is normalised to a usage-weighted GEOMETRIC mean of 100, inside Cameo's own 20:1 window.** §12.0 rule 4's `[10,200]` band is unit-free because it is relative to the matrix's own centre, which is what makes DTA (centre ~590) and OpenRA TD (~61) comparable. ⛔ **Zeros are ABSENCES: excluded from the centre and from the clamp.** Treating a zero as `centre × 0.10` makes the window a runaway — measured on Mental Omega (20.8% zeros) the centre fell 32.3 → 13.5 → 4.1 → 0.2 and never settled, flattening the matrix. Every source above ~13% zeros did it; below ~8% it converged, which is why the flaw stayed invisible until the YR mods were added. |
| R18 | ⚠ **"Geometric mean is 100 and the range is [10,200]" IS NOT A CHECK.** It holds *by construction*, because the matrix is normalised by the very centre being tested — it passed on all six degenerate sources. The independent quantity is the source's own **positive median**: a matrix has collapsed when its typical working value no longer fits inside its own window. `warhead_matrix.py --check`. |
| R19 | **Amends R8 at the ENDS.** R8 holds a value flat beyond the outermost anchor; maintainer 2026-09-21 ruled the opposite for the ladder ends — *"Armor values in between must be interpolated and scout and superheavy must be extrapolated"*. Interpolation and extrapolation are both **geometric** (linear in log space): these are multipliers, so the midpoint of 50 and 200 is 100, not 125. R8's ban on redistributing through Cameo's own §12.0i curve is untouched and still binding. |
| R20 | **One mod, one vote — and Cameo votes too.** Combined Arms (759 warheads) and OpenRA TD (57) each count once; usage weighting decides a mod's INTERNAL centre, never how loudly it speaks. Cameo's vote is cast by its **147 `^Warhead_*` templates**, not the resolved weapons, whose un-retrofitted legacy tail would be a vote for the status quo. This extends R4's equal-thirds principle to the armour layer. |
| R21 | **A reference warhead's family is assigned BY HAND from the compression groups, never by matching its name.** Name matching failed three times in one session and the last one is the clearest: `BazAP` was classified as Tesla because `Ba`**`zAP`** contains "zap"; `ChronoBeam` and `LocomotorBeam` matched "Laser" and are mind-control effects with no damage profile. Delivery comes from `Projectile:`, element from `DamageTypes:`, platform from the actors that fire it — all measured. ⚠ Most `*Death` tokens are DEATH ANIMATIONS, not elements: CA's `HonestJohn` rocket artillery carries `FireDeath`, so Fire requires the real `Incendiary` tag. |
| R22 | **One warhead per weapon on the reference side too.** 116 of Combined Arms' 466 armed weapons carry more than one damage warhead; entering each as a separate weapon put one weapon in three groups. Mirrors DESIGN.md §11b for our own side. ⛔ **AMENDED by R23** — the half of this ruling that said *how* to reduce a weapon to one row is struck: ~~`Warhead@1Dam` is the main in 550 of 577; where the convention is absent the largest `Damage` decides~~. That count came from a scan that treated every warhead node as a weapon, and picking by name reported false immunities. Fold, do not pick. |
| R23 | **A weapon's profile is the SUM of its warheads, not its biggest one.** The maintainer queried four groups in the 2026-09-21 review — *"why is light immune?"*, *"how is the damage so low against None?"*, *"none immune is a bug"*, *"anti light only at 18 while the rest is all at 176?"* — and all four were one measurement defect. OpenRA fires **every** warhead on every hit, and CA's extra warheads are routinely COMPLEMENTARY: `FireballLauncher` states `Light: 0` on `@1Dam` and `Light: 50` on `@2Dam`; `JDAM` and `MaverickSU` put their whole anti-infantry half on `@2Dam`; `ApocRadBeamWeapon`'s `@1Dam` is an infantry-only `HealthPercentageDamage` rider. So fold by effective damage — `versus(A) = 100 x SUM(damage_w x versus_w(A)) / D`, `D = SUM(damage_w)`, a warhead that cannot reach `A` contributing 0 to the numerator and still counting in `D`, which keeps the row RATIOS equal to real effective-damage ratios. ⚠ A per-armour denominator (a plain weighted average) flattens exactly the weapons that split their target sets. ⚠ `HealthPercentageDamage` is a different currency (`HP x Damage/100 x Versus/100`) and folds only with its own kind. Implemented as `warhead_matrix.fold_by_weapon`. |
| R24 | **A percentage warhead is priced through the typical unit it hits.** Maintainer: *"percentage warheads need to be regarded as a flat damage against the average unit of that armor type, so 300% against the average of 50k HP light armor means it's 150k damage against light"*. `HealthPercentageDamage` resolves as `HP x Damage/100 x Versus/100`, so its damage against class `A` is `avgHP(A) x Damage/100` — armour-DEPENDENT, which is exactly the property that makes a percentage weapon relatively better against heavy classes. `avgHP` is the GEOMETRIC mean HP of the buildable actors declaring that armour (CA: None 6,336 · Light 18,774 · Aircraft 23,524 · Concrete 48,973 · Heavy 52,994 · Wood 89,166). Excluding percentage warheads instead — the first attempt — made a sniper whose entire anti-infantry punch is a 300%-of-HP warhead read as a mild anti-vehicle gun. ⚠ Overkill is deliberately NOT modelled, on the maintainer's instruction; the flat side is not capped either, so capping one would bias the comparison rather than fix it. |
| R25 | **Reachability and coverage are resolved per ARMOUR CLASS, not per GND/AIR macro.** R16's macro mask is enough to decide `n/a`, and NOT enough to fold: 380mm's second warhead deals 70,000 with `ValidTargets: Infantry`, and summing that into Heavy and Wood — both "ground" — turns a siege gun into a tank killer. So a warhead reaches armour `A` only if some buildable actor declaring `A` carries a matching `Targetable` type, and it contributes in proportion to the FRACTION of that class it can hit. Coverage is not optional: 3 of Combined Arms' 63 Light-armour actors are cyborgs declaring `Infantry`, so without it a sniper's infantry-only rider reached the Light column at full strength and put `Light` ABOVE `None` on a sniper. |
| R26 | **The reference map is ONE ROW PER CAMEO WARHEAD.** Maintainer: *"everything that shares the same cameo warhead type is only listed once in one list ... one group per cameo warhead mapping"*. `compress_warheads.py` answers what distinct behaviours a mod SHIPS (126 groups for CA); `family_matrix.py` answers what each of OUR warheads must BE (32 live families), collapsing the groups onto their assigned family by usage-weighted geometric mean. A per-weapon override beats its group, so a group contributes only the weapons that stayed with it and its usage is split across its destinations. |
| R27 | **An AA armament is not a separate weapon — fold the pair at ACTOR level.** Cameo's shipped dual-armament law is *"AA split = one weapon"*, the air side earning a 1.5x range bonus and anti-air vehicles +100% firepower — a MODIFIER layer, not a second warhead. Combined Arms splits them, and counting the halves separately invented a family: `MissileAA` was the third-largest in the map at 43 weapons, of which **39 were the AA half of a dual-purpose unit** (Flak: 7 of 9; Laser: 5 of 6). ⚠ Only an UNAMBIGUOUS pair folds — exactly one ground and one air armament on the actor. The Venom's pairing is obvious; the IFV carries thirty ground weapons and four AA ones and choosing a partner there would be a guess. The merged magnitude is the USAGE-WEIGHTED GEOMETRIC MEAN of the two, never a sum (they never fire at the same target), and the pair takes its GROUND half's family. Result: MissileAA 33 air-only weapons, Flak 3, and every dual-purpose ground weapon stops reporting `n/a` against aircraft it can plainly shoot down. |
| R28 | **No `LaserAA` and no `LaserAir`.** Both were considered and rejected on measurement. A dual-purpose laser is already expressed by the R27 modifier layer, so encoding it in the Versus table as well would double-count it; and a genuinely AA-ONLY laser has one candidate in the whole corpus (`MantisLaser`; in Cameo, the Cabal Obelisk of Darkness), which does not earn a warhead. ⭐ Generalises: a dual-purpose family per delivery is minted only where a reference mod shows a real, distinct profile — never for symmetry. |
| R29 | **A family's profile pools its member WEAPONS' rows, never their groups' profiles.** A group is a REVIEW device (120 rows a human can read instead of 550), and a per-weapon override routinely sends one member somewhere else. Pooling group shapes drags the whole group along with that one weapon: `MissileAA` came out with a full ground profile (Heavy 105.6, Light 182.7) on the strength of ONE of its 33 members sitting in a ground-capable group, when every other member is air-only and `n/a` there. |
| R30 | **Cameo votes as one mod among the twenty, and uniqueness is resolved only after all twenty are in.** Extends R20 from the armour layer to the warhead layer. Separation applies on COLLISION only, along the axis where the sources disagree most — so today's 8 near-identical CA pairs (`Laser`/`Tesla` 0.084, `Flame`/`Plasma` 0.088, `Bullet`/`Arrow` 0.105 …) are recorded, not acted on: CA is one vote of twenty and a collision on its data alone may not survive averaging. |
| R31 | **NO CAMEO WARHEAD IS AIR-ONLY — the landed-aircraft rule.** Maintainer: *"MissileAA should still be able to damage ground targets but have the highest versus value against air. What if the aircraft lands? Then it counts as ground, and when the missile is still in flight and the aircraft lands it should of course still deal damage."* The MEASUREMENT stays honest — CA's SAM sites really do state no ground opinion, and writing 100 there is the Aircraft-148 defect pointed the other way — but a Cameo warhead has to fire at whatever it is aimed at. So an air-only family's ground rows are DERIVED from the same DELIVERY's measured warheads (a SAM is a missile) and scaled by `AA_GROUND_RATIO` (0.5) of the measured air value, so the air row stays the peak. Those cells are marked `derived_ground` and shown differently on the map. ⚠ A FALLBACK, not a law: most reference mods DO give their flak cannons and SAM sites real ground rows, so these cells should be replaced by measurement once the other nineteen sources are averaged in. |
| R32 | **Pair armaments by DISTINCT PROFILE, not by distinct name.** OpenRA gives one logical weapon several armament entries for mechanical reasons — firing ports, left/right mounts, garrison slots. The Yak carries `ChainGun.Yak.L` and `ChainGun.Yak.R`, byte-identical profiles on two wing mounts, and counting them as two ground weapons made the actor look ambiguous and blocked the R27 fold of its obvious `ChainGun.Yak.AA`. Counting distinct profiles took Combined Arms from 17 folded pairs to 29. ⚠ And one ground weapon can adopt SEVERAL air variants (`HellfireAG` pairs with both `HellfireAA` and `HellfireAA.Cryo`): merge once per GROUND weapon, or the result carries two rows with the same weapon key and every consumer keyed by weapon name silently collapses them. |
| R33 | **A source's armours are ANCHORED to positions on our ladders, then LOG-LINEARLY interpolated.** Implemented as `tools/reference/armor_interpolate.py`. An anchor pins a source armour at an index on one of our four ladders (INF 3 rungs, VEH 5, BLD 3, AIR 4) — exactly (`combined_arms.None` IS our `None`), fractionally (`combined_arms.Light` at VEH 0.5, between Scout and Light), flat across a whole ladder (`combined_arms.Aircraft`, the only source with a real air tag), or excluded (walls, terrain, projectile-interception classes). Unpinned rungs are filled in LOG space, because these are multipliers and the step between rungs is a ratio. ⚠ A ladder with ONE anchor fills only its own rung: a single point carries no slope, and inventing the rest would invent the source's opinion — OpenRA Red Alert genuinely has no second infantry rung and so has no view on Flak. ⚠ EXTRAPOLATION IS CLAMPED to the §12.0 window [10, 200]: the transcribed Dune 2000 table pins its building ladder only at Steel and Concrete, and running that slope down to Wood produced 250. ⭐ `Heroic` is never interpolated — §12.0b makes it the derived column `Plate x Scout / peak`, recomputed after the ladders are filled. |
| R34 | **SHAPE and LEVEL are averaged separately, because the sources do not all vote on every rung.** A plain per-cell mean compares populations of different size and invents an inversion no source contains: `None` had 18 voters and `Flak` only 9, the nine extra `None`-only voters were the low sources (Tiberian Sun 81, Tiberian Dawn 89, Dune 2000 91), and the average came out `None` 109.4 BELOW `Flak` 119.9 — while **8 of the 9 sources with an opinion on both rungs put them the right way round**. §12.0d says a ladder can never invert, so that was a broken measurement, not a finding. ⭐ A source that pinned TWO OR MORE rungs of a ladder votes on that ladder's SHAPE (its own rungs normalised to a geometric mean of 1, which makes sources with different coverage comparable); every source that pinned ANY rung votes on the ladder's LEVEL; the answer is shape x level. Nobody's data is discarded and nobody's absence distorts a rung. All four ladders then come out monotonic from 18 independent sources: INF 112/111/98, VEH 134/117/100/88/78, BLD 106/86/67, AIR 122/116/109/103. |
| R35 | **RESOLVED — a source's vote on a ladder is weighted by how much of that ladder it really has.** An armour that anchors two ladders contributes ONE measured number to both, and that number is dominated by whichever macro type wears the tag most. Combined Arms is the worked example: its `Heavy` anchors our Plate infantry rung AND the top of the vehicle ladder, but of its **101 wearers, 98 are vehicles and 3 are cyborgs** (ENLI/REAP/RMBC — genuinely CA's heaviest infantry, but targeted as `cyborg`+`vehicle`, never `infantry`), so Plate was being set by a number describing heavy tanks. CA was the only source of eighteen to invert its own infantry ladder. ⭐ Fix: weight each source's SHAPE vote on a ladder by the geometric mean of its anchors' wearer share for that ladder's macro type. Nothing is discarded — CA still speaks about infantry, just quietly. The infantry ladder went from 112.1/110.5/98.3 to **112.8/108.7/99.3**, and all four ladders are monotonic. A source with no actor census (the INI dialects, the transcribed table) speaks at full strength: its armour names are already per-macro-type. |
| R36 | **A cyborg is infantry x vehicle and votes HALF on each ladder** (maintainer's ruling, 2026-09-21). DESIGN.md's *Cyborg dual-armor rule* already says why: a CABAL cyborg keeps its infantry-class `Armor` and adds a vehicle `Armor@<role>`, *"so cyborgs count as both infantry and vehicles for weapon Versus tables"*, and the two rows are **AVERAGED, not multiplied** (`AreaDamageWarhead.MultiArmorCombination: Average` — `Plate` 88 with `Superheavy` 10 resolves to 49, not 8). The same pattern covers Future Tech droids and Schwarzer Mond. So in the wearer census a cyborg counts 0.5 toward INF and 0.5 toward VEH, never 1.0 to either. ⚠ This is also why Crystallized Nexus looked like it had an overlapping `Vehicle` tag: it runs the same two-layer scheme, a macro tag multiplied with a weight class. |
| R37 | **Cameo's own vote needs no compression — its template NAME is the family.** `compress_warheads` looked the source id up in `OPENRA_SOURCES`, which lists the REFERENCE mods only, so Cameo's groups came out delivery `Unknown` / band `unused` while R20 and R30 both give it a vote. `cameo_resolved` is fixed by adding this repository's own ruleset to the lookup (174 groups, 173 with a real delivery). The TEMPLATE view needs no fix at all: a `^Warhead_*` template is abstract and has no `Projectile:`, but `^Warhead_Arrow_Heavy` IS the Arrow family by definition. ⚠ That is not a breach of R21's ban on name-matching: R21 forbids inferring a REFERENCE mod's weapon class from its name, where the names lie; these are our own names, which are the vocabulary itself. All 150 templates parse into **50 families** x {Light, Medium, Heavy, Super}, against the 32 Combined Arms needs — the gap is the consolidation target. |
| R38 | **RESOLVED — the Westwood INI dialects carry delivery and element in a different shape, and it is extractable.** `compress_warheads` keys on `Projectile:` and `DamageTypes:`; the INI mods have neither, and the obvious substitute is a trap — Mental Omega's 1,117 weapons name projectiles `InvisibleWork`, `InvisibleHigh`, `CannonInviso`, which are trajectory and rendering variants, not weapon classes. Nine sources compressed to nothing but `Unknown`. ⭐ The real signals sit in three places and every token was COUNTED in the files rather than taken from documentation: **weapon flags** `IsLaser` (MO 145, TI 18), `IsBigLaser`, `IsElectricBolt` (MO 42), `IsRailgun` (TI 25), `IsDetachedRailgun` (MO 93), `IsRadBeam`, `Lobber`; **the [Projectile] section's behaviour** `ROT` (it steers, so it is a missile), `Arcing` (ballistic shell), `Inviso` (MO 121, hits instantly), `Vertical` (a bomb); **warhead flags** `Fire`, `Tiberium` (MO 48, TI 50), `EMEffect`, `Bullets` (MO 71), `Radiation`, `Sonic`, `Temporal`, plus `InfDeath`. ⚠ `InfDeath` IS A DEATH ANIMATION, the same trap as OpenRA's `*Death` tokens, so only the codes unambiguous across TS and RA2 are used — 3 burn, 4 electrocute, 6/7 chemical — and explicit flags always win. The band comes from which registry (`InfantryTypes`/`VehicleTypes`/`AircraftTypes`/`BuildingTypes`) the firing actor is listed in. Result: **17 of 20 sources compress, 1,202 groups across the corpus** (Mental Omega 17 -> 130, Rise of the East 13 -> 105, Red Resurrection 14 -> 104, DTA 9 -> 35). The three that do not are by construction: `cameo` needs no compression (R37), `cameo_resolved` is at 173 of 174, and `d2k_mod` is a transcribed table with no actors, so it votes on the armour ladder only. |
| R39 | **MEASURED, AND IT FAILS — family propagation by the delivery/element/band triple is 19% accurate, and the GROUP, not the method, is why.** R38 left 512 of 1,059 groups needing a ruling and the other 547 as inherited proposals; nothing had checked whether an inherited proposal was ever right. It is checkable for free: a Cameo weapon inheriting `^Warhead_<Family>_<Level>` states its family in the yaml (R37), and **904 (904 when measured 2026-09-22; **1,116** on master 2026-09-23 after the merge wave — `doc_claims: cameo_family_labelled_weapons`) concrete weapons do**, so compressing Cameo's own weapons and asking the propagation to recover Cameo's own answers is a labelled test that needed no maintainer time. Result on the 53 groups with >=3 labelled members: **propagation by triple 19% top-1** (the right family is somewhere in its top-4 candidates 38% of the time), and **nearest-profile-shape is WORSE at 11%** (top-3: 19%). ⚠ But the ceiling is not 100%. At the shipped `tau = 0.50` one group holds a **median of 3 distinct Cameo families**, worst 15, and only **11 of 53 groups are 80% one family** — no single-family label can be right about `BulletHE_Veh`, which is 296 uses spanning CannonHE, Bullet and Demolition. **~61% is what any method is actually playing against.** ⭐ The fix is the clustering threshold, not a cleverer matcher: purity is a clean function of `tau` — 0.50 -> 61% (162 groups), 0.30 -> 67% (241), 0.20 -> 75% (302), **0.10 -> 89% (413)**. ⚠ And usage weighting makes it WORSE, not better (53% at tau 0.50, below the unweighted 61%), because the big groups are the mixed ones: triaging to "just the important groups" reviews precisely the ones a single label fits worst. Harness: `tools/reference/validate_families.py` (`--purity` for the sweep, `--shape` for the alternative). ⛔ **BLOCKS the plan of reviewing 1,059 tau-0.50 groups** — that review would have been recording labels that are wrong about a third to a half of each group's members, and no later averaging recovers from it. |
| R40 | **RULED (tau 0.20 + drive outward) — and the outward fit by SHAPE is a measured dead end, which relocates the whole task.** Maintainer, 2026-09-22: *"a mix of the tau 0.20 and the drive cameo's families outward ... I don't actually know some of those reference mods like red resurrection and ra2 reborn so I can't give my opinion on those, so you should try to first fit everything from all mods into that reference table yourself."* Groups therefore re-cluster at **tau 0.20**, which R39 measured at 75% purity, and the migration is cheap because the clustering is agglomerative and NESTS: of Combined Arms' 118 reviewed groups **76 do not split at all** and carry forward verbatim, 41 split and 4 straddle — ~45 re-reads, not 118. All sources: 1,059 groups at tau 0.50 → **1,712 at tau 0.20** (1,991 at 0.10). ⛔ But matching groups outward to Cameo families BY VERSUS SHAPE scores **3%**, against the triple's 19%, and adding a delivery-compatibility penalty only reaches 4-6%. The cause is structural, measured two ways. **(a) Cameo's family shapes do not separate**: across the 150 live templates 27 PAIRS sit within 0.05 RMS log distance and `Magic`/`Sonic` centroids are IDENTICAL (0.000), so nearest-neighbour among 51 families is a coin flip inside a cluster. **(b) The target table is already made of the thing being matched to it** — Cameo's shipped `Versus` comes from `docs/reference/family_profiles.json` via `gen_weapon_template.py`, so the match is CIRCULAR where a family was measured and meaningless where it was not: the generator's fallback is `table()`, a LINEAR RAMP. ⛔ **THE "33 RAMPS" FIGURE IN THIS ROW WAS WRONG — see R42, which measures 4.** It was counted from the two profile JSONs (10 reference-derived + 8 hand-designed, so 33 of 51 "absent"), and absence from those files does not mean `table()`: 27 of the 51 are `BLEND_FAMILIES` and average their PARENTS' real profiles, which is a third path the count did not know about. The conclusion below still holds — it is just far smaller than stated. ⭐ **THIS RELOCATES THE TASK.** Finding (b) is the deliverable, not an obstacle: the pipeline exists to replace the unmeasured families with measured profiles (R42 says which, and how few), so the unit of work is the **CAMEO FAMILY (51 rows)**, not the 1,712 reference groups — routing family → groups by categorical identity (a `MissileCryo` IS delivery Missile, element Cryo) rather than group → family by resemblance. The existing 10 cleared `min_rows: 8, min_mods: 3` from an older single-machine extractor (`propose_family_profiles.py` over `survey_platforms.py`, which reads `~/Downloads`); the current pipeline carries 20 sources, so that gate should now clear far wider. Harness: `tools/reference/fit_families.py --validate` (kept as evidence — ⛔ its `--all` output is NOT an assignment). |
| R41 | **tau 0.20 is SHIPPED and the reviewed assignment carried forward LOSSLESSLY — 100% of groups, 0 sent back for re-review.** `DEFAULT_TAU` moves 0.50 -> 0.20 in `compress_warheads.py` (rationale in its docstring is now the measured purity table from R39, not the old "below the 25th percentile" heuristic), giving **1,712 groups across 20 sources** and Combined Arms 118 -> 182. The reviewed decisions survive because the clustering NESTS and the migration is carried by **weapon membership, never by group name** — `retau_assignment.py`. Result for Combined Arms: **74 carried, 72 split, 36 all-overridden, 0 straddle, 0 unreviewed**, and the 82 per-weapon overrides carry verbatim. ⭐ The acceptance test is not the group counts, it is that **all 360 Combined Arms weapons resolve to the SAME family before and after (0 changed)**. The 72 `split` rows are re-opened as `proposed` with a note, because the old review saw those weapons mixed in with others — the label is inherited, not re-confirmed. ⚠ Two corrections to the exploratory counts quoted in R40: *(i)* "4 straddle" counted PARENT-GROUP MULTIPLICITY, and only 4 new groups have several parents of which 3 have parents that AGREE, so there was never a decision to make; *(ii)* the last one, `BulletHE_Veh_5`, is also not a straddle, because **a per-weapon override BEATS its group and an overridden weapon must not vote for where it used to sit** — counting it manufactures conflicts. |
| R42 | **MEASURED FROM THE SHIPPED TEMPLATES — there is NO ramp gap. All 51 families are accounted for, and the real deliverable is the AGE of the 10 base measurements.** R40 inferred a gap of 33 ramps from which families appear in `family_profiles.json` / `invented_family_profiles.json`. That inference is invalid twice over. **(i)** The generator has three paths to a profile, not one: a measured entry, a BLEND averaging its PARENTS' real profiles (`gen_weapon_template.finish_blend`: *"a blend ... averages the parents' REAL profiles and cannot drift from what the parents themselves emit"*), and only then `table()`. **27 of the 51 live families are blends.** **(ii)** Measured directly off the resolved `^Warhead_*` templates through `weapon_efficiency.versus_of`, classifying each family's 16 armour rungs: **47 (47 when measured 2026-09-22; **53** on master 2026-09-23 — `doc_claims: cameo_shaped_families`) shaped · 2 flat · 2 even-ramp** — and all four of the non-shaped ones are DELIBERATE and already declared. `Magic` and `Sonic` ship a constant `100` because they *ignore armour by design*: `gen_weapon_template` special-cases them in BOTH `class_tilt` and the heaviness bell (*"a tilt would destroy that"*), `audit_versus_profile.FLAT_BY_DESIGN` excludes them, and DESIGN.md §12.0i records them as the 2 flat families at the ratchet. That also explains R40's finding (a): their centroids are identical (0.000) because both are the same constant — it was never evidence of a ramp. `Nuclear_Super` and `Sniper_Light` are `audit_versus_profile.HAND_TUNED` — authored by hand, generator skips them. ⭐ **So the family-outward task is not to fill holes; it is to REFRESH THE BASE.** Everything shaped rests on the 10 measured families (`Bullet`, `CannonAP`, `CannonHE`, `Flame`, `Laser`, `MissileAA`, `MissileAP`, `MissileHE`, `Prism`, `Tesla`), because the other 27 are blends of them — and those 10 were generated **2026-08-15** by the OLD single-machine extractor (`propose_family_profiles.py` over `survey_platforms.py`, which reads `~/Downloads`), off **6-7 mods per entry**. The current corpus carries **20 sources and 1,712 groups** (R41). Re-deriving those 10 bases from it propagates to all 47 shaped families at once. ⚠ Three of my own claims were retracted to reach this row (33 ramps -> 4 -> 0); each retraction came from measuring the artifact instead of reasoning about which file a name appears in. |
| R43 | **31 of 50 warhead families declare NO element the engine can gate on — including `Flame`, whose own blend children do.** Found while testing whether a family can be routed to reference groups by its CATEGORICAL identity (a `MissileCryo` IS delivery Missile, element Cryo). It cannot, and this is why: element is read from the warhead's `DamageTypes`, and **31 of 50 families carry the identical generic line `Prone75Percent, TriggerProne, ExplosionDeath`** — among them `Flame`, `Cryo`, `CryoBlast`, `FlakCryo`, `BulletCryo`, `CannonCryo`, `MissileCryo`, `Toxic`, `Chemical`, `Laser`, `Prism`, `Railgun`, `PhotonCannon`, `Waveforce` and `Nuclear`. Only **15** declare a real element (Fire or Tesla) and **4** (`BulletChem`, `CannonChem`, `MissileChem`, `Sniper`) read `Plain`. ⛔ **The inconsistency is inside one family tree**: `Inferno`, `BulletFire` and `CannonFire` all carry `FireDeath, Incendiary`, while `Flame` — the BASE family they blend FROM — carries `ExplosionDeath`. ⚠ This is not cosmetic. `DamageTypes` is what the engine filters on, and the mod's rules already consume these tokens: `Incendiary` x3, `FireDeath` x3, `RadiationDeath` x6, `TiberiumDeath` x7, `AffectedByDamageTypes: Tesla` x2. So a `BulletFire` hit triggers the incendiary rules and a `Flame` hit does not, and nothing gated on radiation can see `Nuclear`. ⭐ It also bounds R39: element is one third of the propagation triple and it is a CONSTANT for 31 of 50 families, so a large part of the 19% ceiling is missing signal in the target vocabulary, not a weak matcher. ⛔ Fixing it edits warheads (rule 4) and needs explicit permission; it is NOT bundled with the reference work. Measured by `cameo_element_bearing_families`. |
| R44 | **SHIPPED — `DamageTypes` is now DERIVED from the family, and the fix is 5 families, not the 31 R43 counted.** Maintainer ruled 2026-09-22: generate it, so a family and the blends made from it can never again disagree about what element it is. `gen_weapon_template.damage_types_for()` composes the line from `ELEMENT_BY_NAME` (the element half of the family name) x `ELEMENT_DAMAGE_TYPES` (element -> tokens), and the three call sites that used to read the hand-maintained dict now read it. ⭐ **The `Tesla` token is appended STRUCTURALLY for every family carrying `IntegrityScale`**, because that is what makes the Integrity trait's passive drain fire — it had been a comment asking the reader to remember, and `Storm` was already relying on a second call site to add it by hand. ⚠ **R43's "31 untagged" is right but only 5 were FIXABLE**: 5 have an element expressible with tokens that already exist (`Flame`, `Thermobaric` -> `FireDeath, Incendiary`; `Chemical`, `Toxic` -> `TiberiumDeath`; `Nuclear` -> `RadiationDeath`), **6 are Cryo and are BLOCKED** because no `FrozenDeath` exists anywhere in the tree — inventing a token is a design decision, not a generator change — and ~20 genuinely have no element in the current vocabulary (`Laser`, `Prism`, `Railgun`, `PhotonCannon`, `Sonic`, `Magic`, `Melee`, `Concussion`, `Demolition`, `Flak`, `Arrow`, `Bullet`). ⛔ **Two regressions were caught by diffing generated output against what ships, not by review**: the first pass also rewrote `Sniper` (whose `RippedApartDeath` is a deliberate signature — a sniper rips the target apart, it does not explode) and `Storm` (which knocks EVERY infantry target prone, not 75%). Both are now declared exceptions in `FAMILY_DAMAGE_TYPES` with the reason written beside them. ⚠ `Nuclear_Super` is `HAND_TUNED` so the generator skips it; its token was set by hand, and the VISIBLE consequence is that nuked infantry now play death animation 6 with the `Poisoned` voice instead of exploding. Result: **13 lines changed across 145 templates, every one a `DamageTypes`, `verify_generator_sync` drift = 0, `find_empty_warhead` = 0, `audit_versus_profile` unchanged at 2/2 expected**. ⚠ The `cameo_element_bearing_families` counter moved only **15 -> 18**, not to 20, and the two-family gap is R45, not a failed edit: `Chemical` and `Toxic` did receive `TiberiumDeath`, but the reference extractor does not recognise that spelling, so it still reads them as `Plain`. |
| R45 | **THE SAME ELEMENT IS SPELLED DIFFERENTLY IN THE TWO MODS, AND THE EXTRACTOR KNOWS ONLY ONE SPELLING.** `compress_warheads.ELEMENT_ORDER` maps Toxin from the token `ToxinDeath`. Measured: **Combined Arms uses `ToxinDeath` 20 times and `TiberiumDeath` 0; Cameo uses `TiberiumDeath` 62 times and `ToxinDeath` 0.** They are the same element under two names, and only CA's is mapped — so every Cameo chem weapon falls through to `Plain`, including the `^Warhead_*Chem_*` families whose entire identity is that element. ⛔ This corrupts the GROUND TRUTH, not just a side table: `cameo_resolved` is one of only two fully labelled sources (R37) and the element axis is one third of the propagation triple R39 measured at 19%. Neither `cameo_resolved` nor `combined_arms` currently produces a SINGLE group classified `Toxin`. ⚠ It is also why R44's counter moved 15 -> 18 rather than 15 -> 20. ⭐ The fix is one line — add `("Toxin", "TiberiumDeath")` to `ELEMENT_ORDER` — but it re-elements warheads, so it changes grouping and therefore group NAMES, and must be followed by `compress_warheads --all --write` then `retau_assignment.py` to carry the review across by weapon membership (R41). ⭐ **FIXED, and the census was the right call — one token was never the whole problem.** Counting Cameo's entire resolved `DamageTypes` vocabulary against the table found **two** unmapped ELEMENTS (`TiberiumDeath` 381, `RA2VirusDeath` 107 — both Toxin; `defaults.yaml` groups all three spellings under one `DeathSounds@POISONED` voice, which is the mod stating they are one element) and **eleven** unmapped NON-elements that belonged in `ELEMENT_NOISE`, led by `Prone75Percent` at **8,055 uses** where only `Prone50Percent` was listed. ⛔ The noise omissions were harmless but the element ones were not, and the mechanism is worth stating: `element_of` subtracts noise then takes the FIRST `ELEMENT_ORDER` hit, so an unmapped element token sitting ALONGSIDE a mapped one is actively **MISLABELLED**, not merely missed — a chem shell carrying `TiberiumDeath, ExplosionDeath` was read as HE. Verified by unit case after the fix: it now reads Toxin. ⭐ **Measured impact: 1,712 -> 1,721 groups**, `cameo_resolved` gains **18 Toxin groups** (Plain -14, HE -7) and `shattered_paradise` **10** (Plain -6), from a corpus that previously produced ZERO. ⭐ **Combined Arms came back BYTE-IDENTICAL** — 182 groups, same names — because it writes `ToxinDeath`, which was already mapped, so the reviewed assignment needed no migration at all and all **360 CA weapons resolve to the same family, 0 changed**. `cameo_resolved` also gains Fire +6 and Radiation +2, which is R44's yaml edit and this fix composing correctly. |
| R46 | **FOUR INDEPENDENT METHODS MEASURED, ALL ~20% AGAINST A 75% CEILING — group assignment is irreducibly a JUDGEMENT task and no tool is going to do it.** Scored against the labelled sets (Cameo's 904 self-labelled weapons; the 337 maintainer-reviewed Combined Arms weapons over 32 families): **propagation by the delivery x element x band triple 17%** top-1 / 34% top-4 (R39 measured 19% on a smaller scored population; tau 0.20 and the R45 element fix grew it from 53 to 71 groups and the accuracy did not move) · **nearest Versus shape 3-11%** (R40) · **direct categorical construction 20%** top-1 / 38% shortlist · **name evidence 17%** overall. ⭐ The ceiling is **75% median purity** at tau 0.20, so every method is capturing barely a quarter of what a perfect single label could. ⛔ The direct construction was the one I expected to work — Cameo's vocabulary IS delivery x element, so a group measured `(Missile, Fire)` ought simply to BE `MissileFire`. It fails because the map is **not injective**: `Bullet`, `CannonHE`, `CannonAP`, `Demolition`, `Concussion`, `Flak` and `Arrow` all measure as roughly `(Bullet, HE)`, and what separates them is the profile SHAPE, which R40 already measured at 3-11%. ⛔ **Name evidence now has a NUMBER, and it confirms R21 empirically**: on the reviewed CA set it guesses for only **42%** of weapons and is right **40%** of the time it does — worse than a coin flip on its own coverage. That is the `BazAP`-read-as-Tesla failure, quantified on real reviewed data rather than argued from three anecdotes. ⭐ **What this settles:** `propagate_families.py` output is a SHORTLIST (34-38% containment), never an answer, and the 1,509 unassigned groups have to be reasoned about per group against their measured signals. The maintainer authorised that work being done here for later review — what it cannot be is automated and declared finished. |
| R47 | **THREE BLOCKERS SAT BETWEEN THE REVIEW AND A SECOND SOURCE, AND ALL THREE WERE DOWNSTREAM OF WHERE ANYONE WOULD HAVE LOOKED.** Found by walking the pipeline before starting `mental_omega`'s 260 weapons, rather than after. **(1) THE COLLAPSE STAGE WAS OPENRA-ONLY.** `family_matrix.source_rows` rebuilt the matrix with `read_openra` and raised *"the INI dialects need their reader"* for anything else — and only **8 of the 17** assignable sources are OpenRA. The other nine include `mental_omega`, `red_resurrection` and `rise_of_the_east`, **the three largest unreviewed sources**. Reviewing any of them would have been real judgement work that nothing could consume. Fixed by reading from `warhead_matrix.collect()`, which already builds every dialect uniformly; verified row-for-row identical to the old path (533 rows, 0 differing values). **(2) `family_matrix --write` OVERWROTE the whole file** with `{source: result}` — correct with one reviewed source, silently destructive with two, because collapsing a second source would have deleted the Combined Arms rows the weapon generator reads. Now merges. **(3) ⛔ THE TOOL WAS NONDETERMINISTIC.** Two runs over identical inputs produced different committed numbers: profile cells moved ~1% and `derived_ground.donors` gained and lost members run to run. Cause: `delivery_of` built a **set** of group names and took `Counter.most_common(1)`, which breaks ties by INSERTION order — so a family whose groups split evenly between two deliveries picked its winner from set iteration order, which varies with `PYTHONHASHSEED` per process. That choice selects the donor POOL a family derives its ground rows from, so a coin flip propagated into the shipped reference data. Proved by running twice under one seed (identical) and once under another (different); fixed by sorting the names and ranking by `(-count, name)` for a total order; verified identical across three seeds. ⭐ The committed numbers did not change — the file on disk happened to be the alphabetical sample — so this bought reproducibility at zero data churn. ⚠ **The lesson is the ordering**: the multi-source layout question had been open in the handoff since before the FIRST source landed, and answering it surfaced all three. Walk the consumers before doing the work, not after. New `assignment_store.py` keys reviews by their `source:` field across `warhead_family_assignment*.yaml` and reports coverage: **2,194 weapons outstanding** across 16 sources, excluding the three that need no review by construction. |
| R48 | **468 WEAPONS ACROSS THREE SOURCES WERE DELIVERED AS "RAILGUN" WHILE BEING MACHINE GUNS — caught by a domain sanity check, not by a test.** The tell was reading Mental Omega's groups before assigning them: **`Railgun/Kinetic` came out ANTI-INFANTRY in 9 of its 11 groups**, which is the opposite of what a railgun is for. The profiles were never wrong — the weapons behind them are `SA`, `KnightSA`, `HollowPoint4`, `GattWH` (small arms, which genuinely ARE anti-infantry) and `ChronoImprisonWH`. It was the LABEL on top of them. ⛔ Cause: `ini_delivery` tested `IsRailgun` **or** `IsDetachedRailgun`. In Ares the second one only MODIFIES the first — it moves where the beam is drawn — so alone it means nothing, and the mods set it on weapons that are not railguns. Censused, the overlap with `IsRailgun` is **EXACTLY ZERO in all three sources that use it**: `mental_omega` 93 set / 93 orphaned, **`rise_of_the_east` 323 / 323**, `ra20xx` 52 / 52. Zero overlap is what proves the flag is not functioning as a railgun marker at all; the genuine railguns (`MutationRailgun`, `ScavengerRailgun`, `RuinerRay`, `AlizeGun`) all carry `IsRailgun` and are untouched. ⭐ **Impact: 1,721 -> 1,710 groups.** Mental Omega's Railgun groups fall **20 -> 4** (Hitscan +12) and the four survivors are all genuine `IsRailgun` weapons; `rise_of_the_east` Railgun -16, `ra20xx` -6, `cnc_reloaded` -1. **Combined Arms is identical again** — it is an OpenRA source and never touched this code path — so the reviewed assignment needed no migration for the third consecutive regrouping. ⚠ **`IsBigLaser` is the same SHAPE of flag and was deliberately NOT changed**: `cnc_reloaded` sets it 40 times with only 14 orphaned, so it is mostly used as intended and the evidence is mixed; `IsRadEruption` is genuinely standalone in Ares. One flag, one measurement, one fix — the R45 census lesson applied without over-applying it. ⛔ **The process point matters more than the flag**: this was the THIRD defect (after R47's three) found by walking the pipeline before starting `mental_omega`'s 260 weapons rather than after. Assigning on top of it would have produced 169 reviewed groups keyed to a delivery that was wrong for a fifth of them, and the error would have looked like a judgement call rather than a bug. |
| R49 | **MENTAL OMEGA IS ASSIGNED — the second source of seventeen, and the first proof the chain runs end to end.** 165 groups / 260 weapons → **246 decided, 14 parked, 16 live Cameo families**, in `warhead_family_assignment_mental_omega.yaml`. ⚠ **Everything is `status: proposed`; the maintainer has reviewed none of it.** ⭐ **How the pass was made, recorded so it can be audited rather than trusted:** decisions were taken once per measured **(delivery, element) SIGNATURE** — 37 of them across the 165 groups — because a signature is what the measurement actually distinguishes, and each carries its reasoning in a `why` field. **14 groups were opened and read individually and BEAT their signature**: `ChronoBeam` (R21 names it — a mind-control effect whose flat 200 across all nine armours is the signature of ABSENT Verses data, not of a generalist), `RepairMechanical` + `RepairMechanicalReju` (repair weapons), `KillDriverWH` (crew removal, it takes the vehicle rather than damaging it), `ChronoImprisonWH`, and two marker warheads sitting at the window floor. ⭐ **The collapsed rows read correctly, which is the real check**: `Bullet` is anti-infantry (83.7 none → 49.4 heavy) over 111 uses and 50 weapons — it is Mental Omega's small-arms mass; `MissileFire` is anti-armour (97 → 195); `Nuclear` is anti-infantry (200 → 58), which is what radiation does; `Demolition` is anti-building (10 everywhere, 200 on wood/steel/concrete). ⭐ **THREE JUNK ROWS WERE FOUND BY DISTRUSTING THE OUTPUT AND ARE NOW GONE.** The first pass produced `MissileAP` reading ANTI-INFANTRY (48.9 vs 12), which contradicts what AP means; opening it showed `HuntressWH` + `KrukovSwordWH` — **blades**, delivered as Missile only because a thrown blade's projectile steers (ROT > 0). They are `Melee`, and the family now carries exactly the right shape. `Railgun` was `AssasinWH` alone, flat 200 across all NINE armours including buildings — the same absent-Verses signature as `ChronoBeam` — and `Chemical` was `BlimpHE`, a detonating blimp whose Toxin element is a death-animation artefact. Also caught in the same sweep: `StunGunWH` (a status effect), `ChronoFreezeWH` and `DiskDrainWH` (markers at the window floor), and `FlintBomb` (a bomb filed as ball ammunition). ⚠ **The lesson is that the collapsed output is the review surface, not the assignment file**: a family whose profile contradicts its own name is a bug report, and all three were invisible until the rows were collapsed and read. ⭐ **Combined Arms was untouched** — the write added 1,062 lines and deleted none. Coverage is now **583 weapons decided of 2,194**; next by usage are `red_resurrection` 147 groups and `rise_of_the_east` 136. |
| R50 | **`EMEffect` IS A MECHANIC, NOT AN ELEMENT — and Red Resurrection looked like a mod built out of electricity because of it.** The tell, again, was reading a source before assigning it: RR came out with `Missile/Tesla` 17 groups, `Bullet/Tesla` 17, `Hitscan/Tesla` 11, `Laser/Tesla` 6. ⛔ In RA2 `EMEffect` makes a warhead DISABLE vehicles; Cameo models that as Integrity/PhysicalState and never as a Versus row — the same class of mistake as R48's `IsDetachedRailgun`. Censused over the seven INI sources it marks ordinary ordnance and almost never a tesla weapon: **`ra20xx` 248 of 684 warheads (36%), 2 tesla-NAMED; `red_resurrection` 110 of 480 (23%), 5 named** — the sample being `105mmWH`, `120mmWH`, `155mmWH`, `ATGUNWH`, tank cannons and artillery — **`mental_omega` 36 of 731 (5%), 0 named**; the other four ~0. ⭐ **Impact: 1,710 → 1,704 groups, and Tesla collapses where it was never real**: `red_resurrection` **Tesla -41** (Fire +26, HE +10, now reading HE 43 / Fire 41 / Plain 37 / Tesla 14 / Toxin 11, a believable composition), `ra20xx` **Tesla -36** (Kinetic +15, HE +9), `mental_omega` -9, `twisted_insurrection` -5. Genuine tesla weapons are still caught, and caught better, at the DELIVERY level by `IsElectricBolt` (23-298 per source) — the right place for it, because being an electric bolt is how the thing is delivered. Combined Arms identical for the fourth consecutive regrouping. ⛔ **SUPERSEDED BY R51 — I called this an open design question on ONE source's evidence and it is not one.** Measuring all seven showed the pattern is identical everywhere, so it is an ordinary bug with a corpus-wide fix, not a per-mod calibration decision. The original text follows for the record: ~~OPEN — the `InfDeath` code table is not stable across mods.~~ `INI_INFDEATH_ELEMENT` assumes 3=burn / 4=electrocute *"unambiguous across TS and RA2"*. Measured on Red Resurrection by correlating each code against warhead names in AGGREGATE (calibrating a code table, not classifying a weapon): **InfDeath=4 is 16 fire-named against 1 tesla-named, and InfDeath=5 is 6 tesla-named against 0 fire-named** — RR uses 4=fire, 5=electro, the reverse of the assumption. That still mislabels ~16 RR flame warheads (`FlamethrowerWH`, `DevilFlamerWH`, `FirestormWH`) as Tesla. A PER-SOURCE code table would fix it; whether the pipeline should carry per-mod calibration is the maintainer's call, so it is recorded here rather than guessed at. ⛔ **AND THE HANDOFF'S OLDEST TRAP CAUGHT ME**: this regrouping retired 9 Mental Omega groups and created 8, and my R49 pass had keyed its 14 individually-read decisions by GROUP NAME — `BombTesla_Inf`, holding the `ChronoFreezeWH` call, simply vanished. Re-authored with all 20 keyed by WEAPON and emitted as `overrides:`, which is what the format was always for and what let Combined Arms survive four regroupings untouched. I wrote the warning into the Mental Omega file's own header one commit before violating it. |
| R51 | **THE `InfDeath` TABLE WAS OFF BY ONE ON BOTH ELEMENTS IT CLAIMED TO KNOW, IN EVERY SOURCE.** `INI_INFDEATH_ELEMENT` read `{3: Fire, 4: Tesla, 6: Toxin, 7: Toxin}` on the stated grounds that those codes are *"unambiguous across TS and RA2"*. Calibrated by correlating each code against warhead NAMES **in aggregate across all seven INI sources** — sound because it identifies a CODE TABLE from hundreds of warheads rather than an individual weapon from its name, which is the thing R21 forbids — the pattern is identical everywhere, `ts` dialect included: **code 4 is fire-named 18/13/26/18/10/16/9 across the seven sources and tesla-named ~0; code 5 is tesla-named 5/11/26/3/2/5/1 and fire-named ~0; code 3 has NO fire correlation anywhere** despite ~450 warheads behind it. ⭐ So the table is `{4: Fire, 5: Tesla}`. Codes 6 and 7 are dropped — zero toxin correlation in any source — and Toxin keeps its real signal, `Tiberium=yes`, carried by 7-132 warheads per source. ⛔ **This is why R50's "needs a maintainer ruling" was wrong**: one source looked like per-mod drift; seven sources showed a single wrong constant. **Measure the population before escalating a decision** — the same rule as `count-before-describing`, applied to whether something is a judgement call at all. ⭐ **Impact: 1,704 → 1,681 groups.** Red Resurrection alone had ~120 warheads mislabelled (82 wrongly Fire from code 3, 16 genuine flamethrowers wrongly Tesla from code 4) and now reads HE 77 / Plain 35 / Fire 14 / Tesla 9 against its 16 fire-named warheads. Also moved: `cnc_reloaded` Fire -18 HE +17, `mental_omega` Fire -13 HE +18, `ra2_reborn` Fire -13 HE +15, `twisted_insurrection` Fire -10. ⭐ **Combined Arms identical for the FIFTH consecutive regrouping**, and this time **all 21 Mental Omega per-weapon overrides survived** — the R50 fix working exactly as intended, where the group-keyed version had lost one. |
| R52 | **RED RESURRECTION IS ASSIGNED — the third source of seventeen — and it shows that even a TRUSTWORTHY flag can be cosmetic.** 137 groups / 195 weapons → **189 decided, 6 parked, 11 live Cameo families**, all `status: proposed`. It could not honestly be assigned before R48, R50 and R51: at the start of this work it showed 17 `Missile/Tesla` groups and 41 `Fire` groups against **two** `Fire=yes` warheads. It now reads HE 77 / Plain 35 / Fire 14 / Tesla 9. **I held it back three times and each wait found a defect that would otherwise have become a reviewed decision.** ⛔ **The new lesson refines R48.** That ruling established `IsRailgun` as the trustworthy flag and `IsDetachedRailgun` as noise. True — but a genuine flag is not a genuine weapon: collapsed, RR's `Railgun` family came out **ANTI-INFANTRY (155 falling to 66)**, which contradicts what the family is for. Reading its five `IsRailgun` weapons one at a time, **four are autocannons and infantry guns** (`SlaveMinerWH` INF 200 / VEH 94, `HeadhunterGunWH` 192 / 71, `InfCannonWH` 162 / 62, `CazadorAutocannonWH` 170 / 74) wearing a railgun BEAM VISUAL, and only `EnforcerWeaponWH` (INF 74 / VEH 88) leans armour. Reassigned to `Bullet`, and `Railgun` now rises 44 → 88 → **132 against heavy**, which is the shape the family exists to carry. ⭐ **THE PROFILE IS THE ARBITER, NOT THE FLAG** — a flag says what the modder drew, the Versus row says what the weapon does. ⭐ Other individually-read calls: `DanteSniperWH` → `Sniper` (200 across all three infantry rungs, no reach against structures at all), `PerunLightningWH` → `Tesla` (a lightning strike delivered vertically, which is why its signature said Bomb), `AntiB` → `Demolition`, plus five repair/heal/deploy warheads dropped. ⚠ Two rows carry a REVIEW note in the file: `Mechanical` (a repair analogue, or anti-mechanical damage — the name does not say) and `VortexS1WH` (assigned `Nuclear` on its measured Radiation delivery although the name suggests a chrono vortex; kept on the measurement, per R21). Coverage: **772 weapons decided of 2,194**, three sources of seventeen. |
| R53 | **RISE OF THE EAST ASSIGNED — fourth of seventeen, and the R52 check paid for itself three more times.** 135 groups / 225 weapons → **223 decided, 2 parked, 16 live families**, all `status: proposed`. This is the source R48 hit hardest — 323 of its weapons carried `IsDetachedRailgun` — and after that fix it has **no Railgun signature left at all**; the one `Railgun` row it does produce came from a weapon delivered as LASER. ⭐ It is also the first source to yield **`Sonic`**, a Cameo family that R44 noted had no delivery cell built: `DisruptorWH`, `SonicWH` and `SonicBurstWH` give it 5 uses and a real shape (189 infantry / 128 vehicle / 148 building). ⛔ **Three rows contradicted their own names and were corrected by reading the profile (R52):** `HollowPointV` measured delivery **Radiation**, which a hollow-point round is not — its profile (50/41/31 infantry rising to 158/200/200 vehicle, no building reach) is an armour-piercing round, so `CannonAP`, and that family now carries exactly that shape. `RailShot2` was delivered as **Laser** — the beam-visual problem again — and is `Railgun` on its 63→200 armour climb. `AngelWH` sat on the Missile/Kinetic signature, which maps to `MissileAP`, but is flat 200 across every infantry AND vehicle rung: a generalist, and collapsing it as AP gave that family a flat 200, which is not what AP means. ⚠ Two rows carry a REVIEW note: `TerrorBombWH` (a suicide bomb, flat 200 on all nine armours — the family is a judgement, the profile must not set it) and the `Demolition` row it dominates. Also dropped: `ChronoBeam2` (R21's named family of weapon) and `SpotterWH` (a targeting relay). Coverage: **995 weapons decided of 2,194**, four sources of seventeen. |
| R54 | **THE ELEMENT VOCABULARY WAS CENSUSED ACROSS EVERY SOURCE, AND THREE WRITERS WERE FOUND THAT FAIL SILENTLY.** ⭐ **The census.** R45 established that one element is spelled differently in different mods and fixed Toxin's second and third spellings. Romanov's Vengeance forced the general case: it showed **ZERO Toxin groups** while shipping a whole Yuri chemical arsenal. Every `*Death` token in all eight OpenRA rulesets was then counted against both lists. **Three additions, each verified against its carriers, not guessed:** `VirusDeath` → Toxin (RV, 36 nodes / 24 weapons, and **every single carrier is a toxin weapon** — ToxinSprayer, Virusgun, PoisonSting, ToxinBomb, ToxinSplash, CloudDamage, GasCrateExplode); `PoisonDeath` → Toxin (CA, 10 / 8 — ChemDebris, CorrupterSpew, VirusCloud, and `SNIPER.ASSA`, whose own warhead **spawns a `viruscloud` actor**, read rather than assumed); `OrangeRadiationDeath` → Radiation (8 carriers, each the Orange twin of a `RadiationDeath` weapon that RV pairs explicitly — NukePayload/NukePayloadOrange, atomic/atomicOrange, RadBeamWeapon/RadBeamWeaponOrange). ⛔ **AND SEVEN REJECTIONS, WHICH MATTER MORE.** `FlameDeath` (RV, 61 weapons) is the most tempting miss in the corpus and is NOT an element: it is on the flamethrowers, but also on CurtainRifle, PsychicJab, MirageGun, IonCannon and every barrel explosion — **17 of 61 carriers burn nothing**. It is the burned-corpse animation, exactly like `FireDeath`, which has been noise since R45. `ElectroDeath` (73) is the same trap wearing the opposite element — ElectricBolt and CoilBolt, yes, but also PrismShot, PrismSupport, Comet and DiskLaser; mapping it to Tesla would have bought **nothing** (every real tesla weapon already reads Tesla from its DELIVERY) and cost the Prism tower. `EnergyDeath` (SP 70 / CN 10 / TS 4) spans lasers, plasma, ion, railgun, tesla **and artillery** — one animation over four elements names none of them. `PsychicDeath`, `BruteDeath`, `MutationDeath`/`MutatedDeath`, `ChronoDeath` and `CabalDeath*` likewise. ⚠ **Adding a token to `ELEMENT_NOISE` changes no classification** — `element_of` subtracts NOISE then looks for an `ELEMENT_ORDER` hit, so a token in neither list already loses. What the set records is that a token was **censused and judged**, which is the only thing that stops the next census redoing the work. **Impact: 1,681 → 1,680 groups**; RV gains 6 Toxin groups and its Radiation pairs merge; CA gains 1. ⚠ **One doubtful row, left alone:** CA's `FireDebris`, a napalm shard sitting directly beside `ChemDebris` in the same file, carries `PoisonDeath` and now reads Toxin. It looks like an authoring slip in CA, and CA's review is the MAINTAINER's to change. ⛔⛔ **THE THREE WRITERS.** *(a)* `compress_warheads.py --write` **replaces** `warhead_groups.json` and does not merge, while `--source` **defaults to combined_arms** — so a bare `--write` wrote one source's 182 groups over all twenty, destroying **1,499 groups**, and printed only `wrote docs/reference/warhead_groups.json` with exit 0. `--write` now requires `--all`. *(b)* `retau_assignment.py` held a **hardcoded path to Combined Arms' file** from when CA was the only reviewed source, and `--source` switched only the GROUPS lookup — so `--source mental_omega` compared MO's new groups against **CA's review** and reported a confident migration of the wrong document. It now resolves through `assignment_store`, and `--source` is required. *(c)* its *"both groupings are at tau X — nothing to migrate"* was **wrong**: a vocabulary change regroups too, and worse. ⛔⛔ **THE FOURTH DEFECT IS THE ONE TO REMEMBER: A SURVIVING GROUP NAME IS NOT A SURVIVING DECISION.** Pulling `CorrupterSpew` out of CA's `Bullet_Veh_7` **cascaded the numbered suffixes**, so `Bullet_Veh_7`, `_8` and `_9` each silently inherited the next group's weapon and three **maintainer-reviewed** decisions attached to weapons nobody had looked at. `assignment_store.coverage()` reported **one** open row out of four wrong ones, because a row is only "open" when its NAME disappears. Every assignment row has always recorded the WEAPONS it was written about, precisely so it could be audited — and nothing audited it. `assignment_store.stale_rows()` now does, and exits 1; **it was tested against the broken state before the fix was trusted** and fires on 4 rows there, 0 after. All four reviewed sources then migrate at **100% carried, 0 straddles, 0 unreviewed** — Combined Arms for the SIXTH consecutive regrouping. |
| R55 | **ROMANOV'S VENGEANCE ASSIGNED — fifth of seventeen, and the first source whose element axis had to be REBUILT before it could be read.** 99 groups / 500 weapons → **408 decided, 92 parked, 18 live Cameo families**, `status: proposed`. Coverage: **1,540 of 2,554 weapons, 60%**. An OpenRA mod, so R48/R50/R51 (all INI-dialect fixes) do not touch it; R54 was written for it. ⭐ **THE JUDGEMENT THE SOURCE TURNS ON: its lasers are not nuclear.** Four groups / 30 uses read `Laser` delivery with a `Radiation` element, which would have made a third of RV's energy weapons radiological. `RadiationDeath` has 11 carriers in RV and they split cleanly: **six are genuinely radiological** (NukePayload, atomic, FalloutBomb, RadiationMissile, RadCrateExplode, RadBeamWeapon) and **five are lasers** — ATLASER, DoomLaser, LunarLaser, DiskLaser, LaserShot — fired as `Projectile: LaserZap` with `Report: vlazatta.aud` and a `laser_explosion` effect. The radiological six reach the table through Radiation, Bomb and Missile delivery, so nothing is lost. ⚠ **THE PROFILE COULD NOT ARBITRATE HERE, AND THAT IS WORTH NAMING:** three of the four groups are FLAT, so R52's rule has nothing to bite on. When the profile is silent the DELIVERY, the projectile, the sound and the impact effect decide — and they are still evidence, not a name guess. ⭐ **R52 CAUGHT ONE MORE, AND ONLY IN THE COLLAPSED TABLE.** `MosquitoMissile` and its three variants were assigned `MissileChem` on their `VirusDeath` tag; collapsed, `MissileChem` read **None 11 / Flak 11 / Plate 10 against Medium 74** — a chemical weapon that ignores people and hunts vehicles, which is the family's definition inverted. The source settles it: an aircraft weapon, `Projectile: Missile` at Speed 512 with Burst 2, stating None 15 / Plate 10 / Light 50 / **Medium 100** / Heavy 75. It is an anti-armour air-to-ground missile, and the reason for the toxin tag is visible one line up in the same node — `ContrailStartColor: A8FFA8`, a **pale green trail**, so the modder picked the green corpse animation to match it. Reassigned `MissileAP`, which now reads None 16 / Plate 12 rising to Medium 109. ⛔ **REVIEW THE COLLAPSED TABLE, NOT ONLY THE REVIEW** — this one survived the per-weapon pass and died in the pool. ⭐ **A domain check that did NOT fire, recorded because a passed check is evidence too.** RV's ELITE tank shells read INF 27 → 149 while VEH holds at 136, which looks exactly like an extraction fault. It is not: `105mm` states `None: 20` and `105mmE` restates `None: 100`. RV really does give elite shells anti-infantry capability. ⭐ **Other individually-read calls:** `MagneRifle` → `Railgun` (INF 27 / VEH 136 — a magnetic rifle that kills vehicles and barely scratches infantry is a coilgun); `APTusk` → `MissileAP` (measured `Bullet` delivery, but the source says `Inherits: ^Missile` with `rocket1.aud`); `Comet`/`SuperComet` → `Prism` (named for neither, settled by `Report: vpriatta.wav`); `Flamer`/`MolotovCocktail`/`TankFlamer` → `Flame` **on profile and name alone**, since R54 left RV with no fire element at all — and the collapsed `Flame` row came out None 110 / Flak 112 / Plate 112 against Heavy 11 / Medium 11, the flamethrower shape exactly, which is the call verifying itself. ⛔ **106 of 500 weapons are dropped as support, targeting or COSMETIC** — the largest such block in any source. 30 of them are RV's `*Line` weapons: `Inherits: ^LineProjectile`, `Warhead@Line: TargetDamage`, no Versus. They draw the tracer for a gun **already in the table under its own name**, and every one would otherwise have voted flat on a real family. ⚠ **4 parked for review:** `CurtainRifleAP` is flat on all thirteen armours and its name says both `Curtain` (invulnerability) and `AP`. Parked rather than guessed. |
| R56 | **`Tiberium=yes` IS NOT AN ELEMENT — the fifth instance of this class, and the one that had been live the longest.** R51 retired the `InfDeath` codes 6 and 7 from Toxin and said *"Toxin keeps its real signal, `Tiberium=yes`"*. It does not. Censused over the seven INI sources on REAL warheads only — sections a weapon actually points at, not the `TIB01`–`TIB20` FIELD OVERLAYS that inflate a naive count to 198 in CnC Reloaded alone — it is carried by **378 warheads of which 15 are toxin-named, 4%**: `ARTYHE`, `BlimpHE`, `NukeWH`, `40MMHE`, `APOCHE`, `CRNUKEWH`. In TS/RA2 the flag means the warhead INTERACTS with Tiberium — detonates a field, hurts tiberium-armoured things — so the mods set it on anything with a real explosion. It made CnC Reloaded's artillery, mortars, a railgun prototype and a sonic warhead, **all named `*HE`**, read as chemical weapons. ⭐ **The replacement was found the same way R51 found its table:** scoring every INI key against whether its carriers are toxin-NAMED, pooled over all seven sources. `Poison=yes` scores **8 of 9 (89%)** — tiny and perfect — and **`InfDeath=8` scores 23 of 51**, whose carriers are `PlagueWH`, `VirusGas`, `AnthraxWH1/2`, `ChemWH`, `DrThraxWH`, `ToxicMolesWH`, `Gas`, `ToxinWH`, `GooCannonWH`. Code 8 is the die-of-gas animation. `Tiberium` scored below every one of the top sixteen keys. **Net: 333 warheads lose a wrong Toxin label, 43 gain a right one; 1,680 → 1,660 groups.** ⚠ **A REMAINING GAP, recorded rather than papered over:** Twisted Insurrection's real chem weapons (`ChemBurst`, `ChemSpray`, `Gas`, `ToxinBomb`, `BlueTibWH`) carry `InfDeath=1` and `Tiberium=no`, so they read `Plain` — **and they did before this change too.** TI has no machine-readable element for them at all and will need per-weapon overrides, the way Romanov's Vengeance's flamethrowers did (R55). ⭐⭐ **AND THE GENERAL RULE THIS RULING BUYS: FOR A BEAM WEAPON, THE DELIVERY BEATS THE ELEMENT.** R55 made that call for one source on its projectile chain. It is now MEASURED, on the only corpus where the answer is known: Cameo's own weapons, whose family R37 reads off the inherit chain. Of **119** Cameo weapons with an energy delivery, **the DELIVERY names the family 74 times (62%) and the ELEMENT 28 (24%)** — a 2.6x edge. ⚠ **It is a vote, not an oracle**, and the same measurement says why: 9 of the misses are `Laser/HE` weapons that are really `Bullet`, i.e. R52's beam-visual-on-an-autocannon, and 9 more are cases where the ELEMENT was right and the delivery was the decoration. 62% is exactly the sort of number R46 was measuring when it found automatic matchers at ~20% against a 75% ceiling: good enough to break a tie, never good enough to skip the reading. ⛔ **Two stale statements struck in the same commit**, per the rule the doc-claims audit preaches: the `ini_element` comment still said the `InfDeath` table was *"OPEN, NOT FIXED… 3=burn / 4=electrocute"*, which R51 superseded three rulings ago and which nothing greps for. |
| R57 | **CNC RELOADED ASSIGNED — sixth of seventeen.** 92 groups / 136 weapons → **102 decided, 34 parked, 19 live Cameo families**, `status: proposed`. Coverage: **1,676 of 2,554 weapons, 66%**. A YR-engine recreation of Tiberian Sun and Tiberian Dawn, and the source that forced R56 — before it, 16 of its 97 groups read Toxin and its only actual chemical weapon is `ToxinWH`. After: **Toxin 16 groups → 1**, HE 25 → 32, Fire 6 → 9, and the collapsed `BulletChem` row reads INF 195 against VEH 49/39/29 and BLD 49/29/20, which is what a chemical weapon is supposed to look like. ⭐ **The `Laser/Tesla` signature — 5 groups, 35 uses, the second largest in the mod — is lasers**, not electricity and not superweapons. I read `TWSuperWH`, `FSSuperBlueWH`, `RedeemerSuperWH` as SUPERWEAPON warheads from their names and was wrong; the INI says `IsLaser=true`, `AnimList=MINILASERMUZZLE2`, and `LaserFragment` names `TSSuperRedWH` directly. "Super" is the upgraded TIER of a laser weapon. The Tesla is `InfDeath=5`, the electrocution animation — right in general (R51), cosmetic here, and settled by the R56 delivery rule. ⭐ **R52 caught one more, again only in the COLLAPSED TABLE:** `ReaperHE_WH` sat in `MissileFire` and made that family read INF 49/61/74 against heavy 200 — a fire missile that cannot hurt people and kills tanks, which is the family inverted. Its own name says HE. Reassigned `MissileAP`. **That is twice in two sources that the per-weapon review passed something the pool caught; the collapse is not a formality.** ⭐ Clean shapes worth recording because they validate the calls rather than merely following them: `CannonAP` INF 49/49/35 rising to 148/191/197 across the vehicle ladder; `Sniper` INF 196 against a flat 44 on every vehicle rung and 30 on every building; `Flak` INF 200 / light 152 / medium 70 / **heavy 29**, which is flak doing exactly what flak does. ⛔ **34 weapons parked**, and the mod documents one of them itself — `[Controller]` is commented *"Mind control warhead. Will skip normal damage like EMP did"*. Six EM-pulse warheads join it (R50: `EMEffect` disables, it does not kill), with the Magnetron, a hijacker, `LIMPY` (a flat 10 on every armour it reaches — it cripples), the parasite dogs, a chrono beam and nine targeting/status weapons including one named `NeverUseDummyWH`. |
| R58 | **SHATTERED PARADISE SHIPS FIVE DEAD ARMOUR TAGS, and it is assigned — seventh of seventeen.** 92 groups / 210 weapons → **196 decided, 14 parked, 18 live Cameo families**, `status: proposed`. Coverage: **1,886 of 2,554 weapons, 74%**. ⛔ **THE DEFECT: `InfantryArmor`, `BuildingArmor`, `VehicleArmor`, `DefenseArmor` and `ConcreteArmor` are named by 3 warheads each and worn by ZERO actors.** Found the way OpenRA's Dune 2000 case already documented in `warhead_matrix` was found — **count who WEARS each tag against who NAMES it** — which took one query and is now the third armour-vocabulary defect in the corpus. The giveaway is that the same three warheads carry BOTH sets with DIFFERENT numbers: `BlackholeblastFinal` states `Building: 20` **and** `BuildingArmor: 80`, `Heavy: 10` **and** `VehicleArmor: 50`. They are a renamed vocabulary's leftovers; the engine reads the live row and ignores the other. This matrix was reading both, so three warheads got five columns of an older draft averaged into their profile and **every other SP warhead got five unstated-100 cells dragging the window centre**. Excluded; 90 → 92 groups, and no other source moved. ⚠ **`Bike` and `None` are KEPT** although no warhead names them, because one actor wears each — the rule is *drop what the engine never looks at*, not *drop what is thin*. ⭐ **SP has NO fire element at all**, the same hole R55 found in Romanov's Vengeance and R56 in Twisted Insurrection: its only element tokens are `TiberiumDeath` (26) and `FireDeath` (99), and `FireDeath` is noise (R54). Its eleven flamethrowers — `DevilsFlame`, `TemplarFlame`, `LightbringerFlame`, `ZhurongCannon`, `RayriderMolotov` — are read from profile and name, and the collapsed `Flame` row comes out **BLD 64 / INF 85 against HEAVY 14 and CONCRETE 12**, which is the call verifying itself. **That is three of seven sources with no machine-readable fire; it is the normal case, not the exception.** ⭐ **R52 fired twice more, both only in the collapsed table.** `SonicClusterMissile` as `Sonic` gave that family a vote of **10 against every ground armour and 148 against Aircraft** — an anti-air missile that happens to be sonic, and Sonic is a family about resonance, not about the sky. Reassigned `MissileAA`. ⚠ **That reassignment has a visible consequence worth a maintainer eye:** SP's other eight AA missiles are air-ONLY, so their ground rows were DERIVED at 0.5x under R31; `SonicClusterMissile` states real ground rows, so measurement now replaces derivation for the whole family and `MissileAA` reads 10 on the ground rather than 74. That is R31 working as written (*"a mod that gives its AA real ground rows will replace them"*), but it means ONE weapon now sets the ground behaviour of NINE. ⭐ **And one case where R52 is deliberately NOT applied**, which is worth recording because the rule is not a reflex: `LynxRocketGas` reads INF 44 against 148 everywhere else, the chemical family inverted — but `LynxRocket`, its non-gas twin, has the **identical** profile. SP swapped the damage type and left the `Versus` alone, so the shape carries no evidence either way and the name and the measured element both say gas. Kept as `MissileChem`, flagged in the file. |
| R59 | **THE TWO DIALECTS RANKED THE SAME SIGNAL AT OPPOSITE ENDS OF THE TABLE, and RA2 REBORN IS ASSIGNED — eighth of seventeen.** 80 groups / 113 weapons → **89 decided, 24 parked, 22 live Cameo families**, `status: proposed`. Coverage: **1,999 of 2,554 weapons, 78%**. ⛔ **THE DEFECT.** RA2 Reborn had **ZERO toxin weapons** while shipping the Virus sniper, the Desolator and a chemical arsenal — the same symptom R56 diagnosed in CnC Reloaded, with a different cause. `Virus`, the virus sniper's warhead, carries **both** `Bullets=yes` and `InfDeath=8`, and `ini_element` checked `Bullets` FIRST, so it read `Kinetic` and sat inside the HollowPoint group — where it was reviewed as **`Sniper`**. ⭐ **`Bullets=yes` is the INI dialect's spelling of exactly the signal the OpenRA dialect ranks DEAD LAST.** `ELEMENT_ORDER` ends with `("Kinetic", "BulletDeath")` under R45's own comment — *"weak: explosive payload vs solid slug, used only when no strong signal is present"* — while the INI path had it beating a specific death code. One concept, two dialects, opposite ends. Demoted below the fallback. **24 warheads move across five sources:** `SAFlame`, `SSABFlame`, `FLAMEWH2*` and `InfernoWH` are flamethrowers that read Kinetic, and `Virus` read Kinetic in **three** sources. ⚠⚠ **I NEARLY REJECTED THIS FIX ON TWO FALSE LOSSES, and the lesson is the oldest one in the lane: READ THE WEAPON, DO NOT GUESS FROM THE NAME.** `BORISWH` and `ThorSSA` looked like a commando's rifle and a heavy gun being wrongly electrified — Boris carries an AK, Thor is a Soviet hero. Opening them settles it: BORISWH is fired by **`EMPAKM_N`** with **`Report=BorisTeslaAttack`**, and ThorSSA carries **`EMEffect=yes`** with **`AnimList=TCCLOUD1B`**, thunderclouds. Both really are electric, so the demotion CORRECTS them. **The change has one real loss, not four:** ROTE's `VLADWH`, an AKM whose CRITICAL throws chaos gas (`Crit.AnimList=NEWCHAOSEXP`) and which now reads Fire on `InfDeath=4`. Overridden back to `Bullet` — the rifle is the weapon and the gas is a secondary effect. ⭐ **VERIFIED IN THE COLLAPSE:** RA2 Reborn's `BulletChem` row now reads **INF 146 against a flat 10 on every vehicle and building rung**, which is a virus rifle exactly, and CnC Reloaded gained a `HitscanToxin_Inf` group for the same weapon. ⭐ Other clean shapes: `CannonAP` INF 33/33/26 rising to 112/137/146; `Sniper` INF 178 against a flat 10; `Flak` INF 200 falling to HEAVY 22; `Nuclear` INF 146 against VEH 55/36/31. ⛔ **60 uses parked**, the largest block being 26 uses of mind control (`Controller`, `XController`) and 14 of `EMPuls`/`LocomotorBeam`/`Special`, all flat. ⚠ **One row left as measured rather than reshaped:** `ShockAWP` reaches infantry ONLY, which is the sniper shape, but its element is a genuine measured Tesla; kept as `BulletTesla` with one use, since R52 corrects a family that contradicts itself and this one does not. |
| R60 | **THE YR WAVE FLAGS, CENSUSED BEFORE ANY OF THEM WAS MAPPED — one admitted, one rejected, one ignored.** `ini_delivery` read `IsLaser`, `IsRailgun`, `IsElectricBolt` and `IsRadBeam` and had **no branch at all** for YR's wave weapons. Counted over the seven INI sources: ⭐ **`IsSonic` — 19 weapons, every one a sonic weapon** (SonicZap, SonicBreak, OldSonicZap, TumbGun, GeneticBeam). The same KIND of engine flag as the four already read; its absence was an oversight, and Twisted Insurrection's three SonicZaps read `Hitscan/Plain` for want of it. **Admitted → delivery `Sonic`.** ⛔ **`IsMagBeam` — 48 weapons, NOT mapped.** It is the magnetic-wave VISUAL, and its carriers are the Magnetron, tractor, scramble and slowdown waves and the Floating Disc's drain (mechanics) *alongside* real damage weapons (TSSonicZap, RaijinZapX, ShadrayWave). One visual over many roles names none of them — the `EnergyDeath` lesson (R54) wearing a weapon flag instead of a death animation. **`AmbientDamage` alone — 221 weapons**, railguns and vulcans included: not a signal. **Impact is deliberately small** — the group count is unchanged at 1,670 — but it renamed the sonic groups in three reviewed sources, and that is where the real finding was. ⛔⛔ **THE AUTHORING AID HAD THE R54 BUG IN IT.** `by_group()` failed loudly on a group NAME that had disappeared, and that was all it checked. Under R60, CnC Reloaded's `BulletSonic_Veh` **still existed but now held `SubmergedEMPuls` instead of `SonicWarhead`**, so the check passed and would have filed an EM pulse under the Sonic family. **A surviving name is not a surviving decision** — written into R54, into LESSONS_LEARNED, and then re-committed one layer up in the tool built to prevent it. `by_group()` now compares each group's CURRENT members against the weapons the committed assignment file recorded for that row, and refuses on any difference. **Proven on the broken state first** (it fires on the reused name, passes on unchanged membership), then all seven proposed sources re-authored clean. The three fragile small-group reads were re-keyed per weapon. |
| R61 | **RA 20XX ASSIGNED — ninth of seventeen.** 77 groups / 125 weapons → **95 decided, 30 parked, 20 live Cameo families**, `status: proposed`. Coverage: **2,124 of 2,554 weapons, 83%**. ⭐ **Its ELEMENT axis is the problem, and every piece of it was read one weapon at a time.** Nine groups read Fire and **only three are flamethrowers** (`FlamethrwWH`, `TowerFlamethrwWH`, `SAFlame`). The rest carry `InfDeath=4` — the burn animation, which R51 calibrated correctly and which is simply cosmetic here — or an explicit `Fire=yes` on weapons that are something else. `DZapWH` is a **Disruptor**: its weapon fires through `Projectile=Sonic` with `Report=DisruptorFireSound` and wave `AmbientDamage=2`, but carries no `IsSonic`, so R60 cannot catch it and the weapon section settles it → `Sonic`. `TeslaBlastWH` is a tesla ball (`TeslaBallProj`, `TeslaBlastSound`) that ALSO sets fires — three signals to one → `BulletTesla`. `BEagleWH` and `MeteorWH` are bombs that burn → `Demolition`; `MIGWH` is a MiG's missile → `MissileHE`. ⚠ **Two parked, deliberately:** `RaijinWH`/`AARaijinWH` carry three readings — the name is the Japanese THUNDER god, the weapon draws the magnetic wave (`IsMagBeam`, a visual per R60) and the warhead burns (`InfDeath=4`) — over a profile flat at 200 that cannot choose. ⭐ **R52 applied twice on names that promised an element the profile does not deliver:** `RadCannonWH` and `NukeGuyWH` are anti-ARMOUR in shape (INF 24–93 against 200 on every vehicle rung) — radiation kills people, not tanks — so both are `CannonAP`. **No R52 contradiction in the collapse**, the second clean source running. ⭐ Clean shapes: `CannonAP` INF 53/53/34 rising to HEAVY 200; `Sniper` INF 200 against 16 on vehicles and 10 on buildings; `BulletChem` INF 200 against a flat 10 — the Virus rifle R59 freed. |
| R62 | **TWISTED INSURRECTION ASSIGNED — tenth of seventeen, and the first source read ENTIRELY per weapon.** 70 groups / 96 weapons → **85 decided, 11 parked, 21 live Cameo families**, `status: proposed`. Coverage: **2,220 of 2,554 weapons, 87%**. ⭐ **Why per weapon:** the element axis carries almost nothing — the chemical and mutation weapons have no machine-readable element (R56), `InfDeath=5` sits on lasers, tachyon guns and plasma alike, and TS fires its tank guns through `Projectile=InvisibleMissile`, so 105mm/120mm measure as MISSILES. All 96 carry an individual reading; the signature table is fallback only. ⭐ **Three R52 calls, all on the profile:** (1) **`AntiInfLaser`, `PollyWH`, `Viper*` → `Prism`.** They are lasers but shaped ANTI-LIGHT (INF 200 falling to CONCRETE 5–13); Cameo's `Laser` is anti-heavy and `Prism` is the same beam mirrored to anti-light. The collapse confirms it: Prism 200/116/97/52/13 against Laser 138/152/200/166/78 — the first source where `Prism` is chosen by SHAPE rather than by name. (2) **`ShockwaveWH` (Force Cannon) → `CannonAP`** — named for a shockwave, INF 43 against 200 on every vehicle rung. (3) **`PlasmaWH` → `Plasma`, not `Railgun`**, although its weapon sets `IsRailgun`: flat 200 on every rung, and a railgun is anti-armour. R57 (delivery beats element) decides between a delivery and an ELEMENT; it never overrides a profile that contradicts the delivery's family. ⭐ **Gas is `Toxic`, not `Chemical`.** `Gas` and `WeakToxinBomb` spawn `GasCloudSys` and hit infantry 100% against 10% on vehicles; Cameo's `Toxic` IS the anti-infantry gas and `Chemical` is corrosion, pointed the other way (`gen_weapon_template.py`). The Infector's mutation gun and the acid spits follow the RA 20XX Virus and SP Weasaurus precedents → `BulletChem`. ⭐ **An anti-air railgun takes the AA family** (`AARailShot` → `MissileAA`): it fires through `InvisibleAA`, and a family that ranks AIR last would mis-teach the average. **EMEffect warheads are dropped (R50)** — in TS an EM warhead's `Damage` is the disable DURATION. **No R52 contradiction in the collapse**, the third clean source running. |
| R63 | **DTA'S `$Inherits` WAS DROPPED BY THE WARHEAD READER — found by reading the sections the first DTA pass could not explain.** Several DTA warheads (`E3APRA`, `BazAPAARA`, `APRA`, `Super3`, `HHAE`) stated no `Modifier.<armor>` at all and measured FLAT 100%. Their sections carry `$Inherits=<parent>` (Vinifera's section inheritance), and `warhead_matrix.KEY_RE` required a key to start with a letter, so every `$Inherits=` line was skipped in silence: **790 DTA sections — 56 warheads, 132 weapons, 315 units — were read without their parent.** `E3APRA` is really the rocket soldier's anti-armour warhead (INF 220% against HEAVY 1250%), not a generalist. ⛔ **The same regex bug was already fixed once**, in `extract_ini_units.py`, whose own docstring cites *"the `$Inherits` regex that silently dropped the DTA directive"* — the fix never reached the second reader. **Fixed by reuse, not a second flattener:** `KEY_RE` admits a leading `$`, and the new `load_dta_ini()` calls `extract_ini_units.resolve_inherits` per file and then layers `Enhance.ini` — the unit corpus's exact order, so the two pipelines cannot read one DTA section two ways. `ini_lookup.py` reads DTA through the same function. **Measured before shipping:** only DTA uses `$Inherits` (0 sections in the seven other INI sources), so every assigned source is byte-identical; `dta_enhanced` 53 → 50 groups / 88 → 83 weapons, `dta_classic` 41 → 38 / 65 → 58 — the phantom flat groups were inheriting warheads counted as distinct behaviours. `assignment_store` exit 0, no reviewed row moved. Coverage denominator 2,554 → **2,542**. (`cameo` / `cameo_resolved` also regrouped in the same run: that is master's W24 folds since the last regeneration, and R37 exempts both from review.) |
| R64 | **DTA ENHANCED ASSIGNED — eleventh of seventeen, the first source read after R63.** 50 groups / 83 weapons → **78 decided, 5 parked, 20 live Cameo families**, `status: proposed`. Coverage: **2,303 of 2,542 weapons, 91%**. Every weapon read on its own with `ini_lookup.py dta_enhanced`. ⭐ **DTA's `Super` is the Obelisk of Light**, as it is in TD: `LaserFire`/`ShipLaser` (`LLine`, `OBELRAY1`, IsLaser), flat 1000% → `Laser`. The warhead also measured Tesla (`InfDeath=5`) and is shared by zero-damage launchers (`BRig5Inch`, `P941Nuke`, `FireAreaInit`), which is why a group read would have misfiled it. ⭐ **Chem spray is `Chemical`, not `Toxic`:** DTA's `Chemical` warhead is INF 440% against MEDIUM 700% — it eats armour, which is Cameo's corrosion family; TI's gas (R62) pointed the other way and went to `Toxic`. The profile, not the word, decides. ⭐ **R52 on a name:** `CommandoGrenadeAP` (the anti-materiel rifle and big grenade) is INF 130% against HEAVY 1000% → `CannonAP`. **The R63 fix paid off immediately:** `E3APRA` and `BazAPAARA` (the rocket soldier's AT and AA rockets) read as flat before R63 and now read as their parents — `MissileAP` and `MissileAA`. TS-engine tank guns measure as Missile again (`AP`, `APE`, `APB` → `CannonAP`). **No R52 contradiction in the collapse**, the fourth clean source running. |
| R65 | **DTA CLASSIC ASSIGNED — twelfth of seventeen, by DIFFERENCE from Enhanced.** 38 groups / 58 weapons → **53 decided, 5 parked**, `status: proposed`. Coverage: **2,361 of 2,542 weapons, 93%**. Classic is `Rules.ini` without the `Enhance.ini` overlay, and all 58 of its weapons also exist in Enhanced, so each Enhanced reading was reused ONLY after comparing the two warheads' `Modifier.<armor>` rows: **18 of 58 differ, and exactly one difference flips a family.** ⭐ **The chem spray (`Chemical`/`ChemicalE`) is `Toxic` in Classic and `Chemical` in Enhanced** — Classic's is INF 440% / WOOD 750% against HEAVY 250% (anti-light), and Enhanced rebalanced it to MEDIUM 700% (armour-eating). Same weapon, same name, two families, because the profile decides per source (R52) and DTA changed the weapon between editions. The other 17 differences move values inside a family (tank guns INF 100% → 190%, grenades, artillery, napalm) and change nothing. **No R52 contradiction in the collapse.** |
| R66 | **OPENRA RED ALERT ASSIGNED — thirteenth of seventeen.** 33 groups / 44 weapons → **40 decided, 4 parked**, `status: proposed`. Coverage: **2,405 of 2,542 weapons, 95%**. Every weapon resolved with its firer through `miniyaml`. ⭐ **The light tank's `25mm` is `CannonHE`, not AP** — Light 116 against Heavy 48 is anti-light, the CannonHE side of the AP/HE split, while the medium/heavy/mammoth guns (Heavy 170–189) are `CannonAP`. ⭐ **An underwater-only weapon is PARKED, not assigned:** `DepthCharge` (`ValidTargets: Underwater`) filed as `Demolition` made that family peak on LIGHT with Concrete low — the submarine's armour row, the opposite of Demolition's shape (R52 caught it in the collapse). A weapon that can hit nothing but submarines carries no unit-vs-unit evidence; the maintainer-reviewed Combined Arms file already parks its depth charges (`park: naval`), which this now matches. Tanya's `Colt45` and the Spy's `SilencedPPK` have no Versus rows but `ValidTargets: Ground, Infantry` → `Sniper`. |
| R67 | **CRYSTALLIZED NEXUS ASSIGNED — fourteenth of seventeen.** 28 groups / 59 weapons → **57 decided, 2 parked**, `status: proposed`. Coverage: **2,464 of 2,542 weapons, 97%**. CN's 14-armour set (Infantry/Cyborg/Light/Medium/Heavy/Superheavy/…/Shield) makes its shapes easy to read. ⭐ **Its tank guns are anti-LIGHT** — one shared warhead, Light 110 / Medium 100 / Heavy 85 / Superheavy 60 → `CannonHE`, like RA's `25mm` (R66). ⭐ **R52 caught its lasers:** the Obelisk, laser turret and Sting of Nod share an anti-LIGHT warhead (Infantry 200, Heavy 84, Superheavy 50) and first collapsed into `Laser`, which is anti-heavy; they are `Prism`, the TI precedent (R62). DTA's Obelisk (R64) stays `Laser` because its warhead is flat — same unit name, different data, decided per source. `Proton` and the `CyclopsLauncher` share a generalist warhead with Shield 350 → `Plasma`; the Acolyte's Tiberium rifle → `BulletChem`. Worth recording for the Cameo stealth-tank question (#455): CN's stealth tank `Dragon` also targets `Ground, Air`. |
| R68 | **ALL SEVENTEEN SOURCES ASSIGNED — the last three vanilla OpenRA mods.** `openra_ts` 22 groups / 32 weapons (30 decided, 2 parked), `openra_td` 20 / 25 (25 decided), `openra_d2k` 15 / 21 (20 decided, 1 parked). **Coverage: 2,542 of 2,542 weapons — 2,273 decided, 269 parked, 0 open.** Every weapon resolved with its firer and real Versus through `miniyaml`. ⭐ **`Plasma` absorbs plasma weapons leaning EITHER way**, because Cameo's `Plasma` is the Flame × Chemical blend (`gen_weapon_template.py`), a generalist: TS `Proton` and D2k's Devastator `DevBullet` lean anti-armour, TS `CyCannon` anti-infantry. ⭐ **Vanilla evidence on the #455 question:** TS's stealth tank `Dragon` targets `Ground, Air`, and TD ships a dedicated `227mm.stnkAA` — the stealth tank shoots aircraft in both originals, as Cameo's does. ⚠ **FOR THE AVERAGING STAGE, not fixed here:** vanilla OpenRA TS actors declare LOWERCASE armour types (`heavy` ×52, `wood` ×20, `light` ×11, `concrete` ×9, `none` ×4) beside the capitalised ones, and no weapon's Versus names them — so in that game those actors take 100% from everything, and the matrix carries five columns that are neutral for every family. They are genuine data, not an extraction bug, but they hold no evidence: the averaging must treat them like R58's dead tags (merge into their capitalised twin, or exclude), never average them as signal. |
| R69 | **AVERAGING WAITS FOR THE MAINTAINER'S REVIEW (ruled 2026-09-23), and `Nuclear` must NOT be averaged as assigned.** The maintainer chose to review the 16 `proposed` sources on the map before any averaging runs; only Combined Arms is reviewed. ⛔ **A cross-source check found `Nuclear` is the wrong home for most of what it holds:** in **7 of 8** sources the collapsed `Nuclear` row is ANTI-INFANTRY — none/flak/plate ≈ 200 against heavy 28–78 and concrete as low as 12 (Mental Omega, Rise of the East, RA2 Reborn, RA 20XX, Red Resurrection, CnC Reloaded, Romanov's Vengeance). Those are RADIATION weapons (the Desolator's beam and eruption, rad cannons); only Combined Arms' single entry is a building-breaking nuke. Cameo's `Nuclear` is buildings-first (`gen_weapon_template.py`: BLD, VEH, AIR, INF), so averaging them in would drag it toward anti-infantry. Cameo has NO anti-infantry radiation family — its own Desolator beam (`RA2RadBeamWeapon`) sits on `Chemical_Medium`, which is shaped anti-armour. A `Radiation` family is proposed to the maintainer with the other missing families; if adopted, these rows re-key to it. Each source's R52 check missed this because the per-source collapse only compares a family with its own name, never with Cameo's shape for that family — a check to add before averaging. |
| R70 | **TWO MAINTAINER-CONFIRMED FAMILIES WERE NEVER BUILT, so later sources bent around them — re-keyed.** The Combined Arms review CONFIRMED `Radiation` (DesolatorBeam, RadTrooperBeam, ApocRadBeamWeapon) and `LaserPulse` (small anti-light pulse lasers; *"Prism is reserved for PrisCLaser/PrisTLaser"*), but neither has a `^Warhead_*` template, so every later source filed the same weapons under the nearest live family: rad beams under `Nuclear` (R69's anti-infantry rows) and anti-light lasers under `Prism` — which the maintainer's own note forbids. **Re-keyed per weapon, verified by effective-family diff: 29 `Nuclear` → `Radiation`** (every rad beam/eruption/eradication, the Desolator, Krukov, RV's rad shells, and the NEUTRON bombs, which are anti-personnel radiation by design), **9 `Prism` → `LaserPulse`** (TI's anti-infantry lasers, CN's shared laser warhead), **1 parked** (`VortexS1WH`: a vortex by name, radiation by measurement), **0 unintended changes**. `NUKELOW` stays `Nuclear`; the real prism weapons stay `Prism`. ⭐ **The rule this teaches: a confirmed family with no template is still the family.** The assignment vocabulary is the maintainer's rulings, not the list of templates that happen to exist today. |

---

## §1 — What we are extracting from

**Two families of source, and they need different extractors.**

**A. INI mods** (Ares/Westwood engines) — already extracted, in
`Cameo-mod-reference/extraction/`, 8183 unit rows:

| source | units | costed | armor rows |
|---|--:|--:|--:|
| Rise of the East 3.0.0c | 2445 | 1666 | — |
| RA2 0XX 1.0.8 | 2104 | 486 | 684 |
| Mental Omega 3.3.6 | 1706 | 786 | — |
| CnC Reloaded 2.7.0 | 1306 | 816 | 355 |
| Red Resurrection 2213 | 1048 | 491 | 480 |
| DTA (Classic + Enhanced overlay) | 869 + 112 | 417 + 85 | `Modifier.*` |
| RA2 Reborn 1.0.31 | 697 | 366 | 176 |

**B. OpenRA peer mods** — 16 declared in `tools/reference/extract_peer_units.py:PEERS`:
`ca` (Combined Arms), `cn` (Crystallized Nexus), `sp` (Shattered Paradise), `rv` (Romanov's
Vengeance), `hv` (Hard Vacuum / OpenHV), `e2140` (OpenE2140), `gen` (Generals Alpha), `fnw`,
`ra2vsh` (Valiant Shades), plus the upstream `ra`, `ra2`, `yr`, `ts`, `cnc`, `d2`, `d2k`.

✅ **A1/A2 DONE 2026-09-05** — `extract_peer_units.py` now runs. **9 of 16 peers resolve, 1946 units**:
Combined Arms 382, Romanov's Vengeance 729, Shattered Paradise 306, Generals Alpha 153,
Crystallized Nexus 97, OpenRA TD 56, RA 94, TS 73, D2k 56 — the last four from the
`cameo-engine` clone, which already ships `mods/{cnc,ra,ts,d2k}`.

⏳ Still missing 7, ranked by the armor map's own assessment: **Valiant Shades** (AS-lineage,
highest confidence, tied with RV), **OpenHV**, **OpenE2140**, OpenRA RA2, Yuri's Revenge on
OpenRA, Fractured Realms, Dune II (declares NO `Versus` — unit stats only). Non-blocking.

---

## §2 — The build order

### Phase A — extraction

| # | task | notes |
|---|---|---|
| **A1** | ✅ DONE — **Port `mod_id` into `tools/audit/miniyaml.py`** | ⛔ BLOCKER. `Ruleset.__init__` takes one arg; `extract_peer_units.py` calls it with two. The change exists on `origin/claude/docs-audit-reorganize-xgzwhr` (27 insertions, `mod_id: str = "cameo"` default, so every existing caller is unaffected). Devin landed the tooling without it. |
| **A2** | ✅ DONE — Resolve all 16 peers on disk; report which are missing | `--dry-run`. A missing root is skipped near-silently — read every line. |
| **A3** | ✅ DONE — INI extractor → the same schema as the OpenRA extractor | RA2/YR 11-slot `Verses=`, TS 5-slot, DTA `Modifier.*`. |
| **A4** | ✅ DONE — Every stat, both families (R6) | core (HP/cost/speed/range/DPS/armor) + build/economy + combat detail + vision/utility. |
| **A5** | ✅ DONE — Every `Versus` / `Verses` / `Modifier.*` row | ⚠ In OpenRA yaml `Versus` is a **node with an EMPTY value whose children are the rows**. `node.get("Versus")` returns empty and yields the false result "0 peers expose Versus". Use `weapon_efficiency.versus_of`. |
| **A6** | ✅ DONE — Armor normalisation per R8 | onto the four ladders in `docs/reference/peer_armor_map.yaml`; confidence gates voting; only `high`/`medium` vote. |
| **A7** | ✅ DONE — Convert the two UTF-16 reference files to UTF-8 | `PEER_ARMOR_VOCABULARIES.md`, `peer_armor_map.yaml` — PowerShell `>` wrote them; every grep silently under-reads them. |

### Phase B — statistics (R5, R7)

| # | task |
|---|---|
| **B1** | ✅ DONE — Per-source, per-faction rosters, split by type: infantry / vehicle / aircraft / naval / defense |
| **B2** | ✅ DONE — Faction "average actor": geometric mean per type **and** overall |
| **B3** | ✅ DONE — The same aggregate for the **whole source game**, per type — the comparison group |
| **B4** | ✅ DONE — Faction profile = B2 ÷ B3, per type and overall, each an independent value |
| **B5** | ✅ DONE — Variance: CV, min/max spread, percentile position — faction vs its own game |

### Phase C — the mapping matrix

| # | task |
|---|---|
| **C1** | ✅ DONE — For each of the 29 real Cameo factions, assign its reference combination (R3, R4) |
| **C2** | ✅ DONE — Record it as reviewable data next to `faction_routes.py`, with the reason per pairing |
| **C3** | ✅ DONE — Verify every Cameo faction reaches the ≥2-reference floor, or is explicitly `UNROUTED` (formula-only) |

### Phase A/B/C — CLOSED 2026-09-06 by wiring the corpus into the distribution layer

`ini_corpus.json` existed from 2026-09-05 but nothing READ it: `reference_distribution.peer_rows()`
parsed two markdown documents only, so `faction_routes.py --check` reported all fifteen INI routes
as *"source not in the de-duplicated corpus"* and `allows()` admitted zero rows for them. The
loader is `reference_distribution.ini_rows()`. Corpus **2,568 -> 4,523 peer rows, 15 -> 21 sources**;
Cameo actors with a reference signature **324 -> 374**.

Four things that had to be settled to land it, each measured rather than assumed:

| what | finding |
|---|---|
| **DOC1 vs the extraction** | `ORIGINAL_UNITS_RAW.md`'s hand-typed Mental Omega / CnC Reloaded tables are the same data, less of it, with typos: median HP ratio 1.000 but MO's Lionheart Bomber reads 10,000 HP against `[LIONH] Strength=800`. DOC1 now stands down per source, automatically, for anything the extractor covers — it currently supplies **zero** rows. The one signature lost (`terran_ghost`) matched an MO row for a unit MO does not have. |
| **`cost > 0` is not a buildability test** | these mods price internal dummies at 1 credit. CnC Reloaded's `TSCARRYALL_DUMMY` is a costed **10,000,000 HP** row against a real ceiling of 6,000. `TechLevel = -1` + `Selectable/IsSelectableCombatant = no` are the engine's own flags and fix every tail (RotE 15,000 -> 2,000, RA2 0XX 9,999 -> 3,000). They also drop the elite/upgraded DUPLICATE actors these mods ship, which were double-counting their own base unit. |
| **route tokens were never case-folded** | `peer_factions()` lowercases the corpus; the INI routes are written in each source's own casing. All fifteen matched nothing while `--check` printed the token it wanted next to the same token, lowercased, in the same line. |
| **"exclusive" means different things per source** | CnC Reloaded has real per-faction pools (nod 86, gdi 61, soviet 42, allies 41, yuri 32). **Mental Omega has almost none — 3 to 7 per country** — because it models four SIDES; country exclusivity cut `japan` to 6 units. Exclusivity is now a declared PARTITION per source, and `faction_routes` and `faction_profile` share one copy of it. Worst Cameo-faction roster overlap **97% -> 49%**. |

### Phase D — virtual anchors (R1, R2)

| # | task |
|---|---|
| **D1** | Migrate `docs/balance/class_anchors.json`: drop `anchor_actor` / `verifier_actor`, keep `spec` as the definition at 100% of the band |
| **D2** | Write the resolver-check audit: resolve **every** unit in a class, assert the formula reproduces its price |
| **D3** | Update `FORMULA_V2.md` §2 — it currently rules that every class has a *living baseline unit in game*, which R1 replaces |

---

## §3 — Traps already paid for. Do not rediscover these.

1. **`git grep` skips several of our weapons yaml as binary** (non-UTF-8 bytes), and **`miniyaml`
   silently under-parses the same files.** For any presence check use
   `git show <rev>:<file> | grep -a`. This nearly deleted 30 live weapon nodes.
2. **A mod's loose `rulesmd.ini` can be vanilla Yuri's Revenge byte-for-byte** — Mental Omega's is
   (md5 `cf7eb658327aff1fe7e6c4e7400eb87f`). Check every extraction against that hash.
3. **`extract_mix_ini.py` sniffs only the first 4096 bytes**, so a rules file with a comment banner
   reports "0 INI blobs found". Judge blobs by full content.
4. **`Versus` is an empty-valued node** — see A5.
5. **A claim about the corpus is not a claim about the world.** "MO and CnC Reloaded are not
   recoverable" was recorded as fact in `REFERENCE_PIPELINE_HANDOFF.md` §1.3; both were on disk.
