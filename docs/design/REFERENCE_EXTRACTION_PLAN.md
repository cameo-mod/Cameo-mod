# Reference extraction & faction mapping — the plan

**Owner: Claude-Local.** Lane: `tools/reference/**`,
`tools/balance/{assign_references,faction_routes,faction_extrapolate}.py`, `docs/balance/review/**`,
`docs/reference/**`. No other agent should edit these while this runs.

This is the build order for turning the reference corpus into per-class targets. It supersedes
nothing — it *implements* `REFERENCE_METHOD.md` and the maintainer rulings of 2026-09-05 recorded
in §0 below.

---

## §0 — Maintainer rulings, 2026-09-05. Do not re-litigate these.

### September 9 follow-up rulings

Aedis's direct instructions for the Japan pilot update the execution of the earlier
rules, not their raw-source or one-roster/one-vote safeguards:

- Initial reference placement includes **stats and price**. Classic original prices
  may change; subsequent fitting may adjust stats and price jointly on the existing
  permitted grids. A near-zero formula delta is not gameplay validation.
- Implement source-relative versus fitted-target separation; freeze pilot calibration
  inputs; expose counterpart/analogue/extrapolation status and disagreement; validate
  withheld references; and compare representative counter/tier/TTK relationships before
  recommending live changes. These are distinct checks, not one numeric acceptance score.
- MCVs and harvesters are collection-only/manual-review subjects. Harvesters need their
  separate formula. Neither may silently enter automatic combat-unit fitting.
- Extract RA3, Dune II and Emperor reference evidence; obtain versioned Spice Wars data.
  Exact DTA re-extraction awaits the files Aedis will provide. Unsupported damage channels
  and unresolved source dependencies must remain visible and must not become complete
  numeric evidence merely because a column is present.

Instruction timestamps, implementation boundaries and pending evidence are recorded in
[the September 9 ruling record](../balance/review/AEDIS_OVERNIGHT_RULINGS_20260909.md).
These instructions do not sign off anchors or authorize merging PRs.

### Earlier rulings

| # | ruling |
|---|---|
| R1 | **Class anchors become VIRTUAL.** No class is anchored to a real actor any more. An anchor is a pure number at the **100% point of the price band** (the band is `FORMULA_V2.md` §3's 50–250% envelope of `C₀`). |
| R2 | **The verifier goes virtual too**, and is replaced by a stronger check: a new audit resolves **every** unit in a class through `miniyaml` and asserts the formula reproduces its price. This is strictly stronger than the single 2.5×`C₀` tripwire unit it replaces. |
| R3 | **Faction mapping is a unique SET, members may repeat.** No two Cameo factions share the same combination, but one reference faction may appear in several combinations. The existing law that a reference *unit* is spent once is unchanged. |
| R4 | **Equal thirds, and current Cameo yaml is always one of the voices** — `Cameo TD GDI = DTA GDI × Combined Arms GDI × current Cameo TD GDI`, each 1/3, synthesised by geometric mean. |
| R5 | **Faction profiles are computed per TYPE and overall** — infantry, vehicle, aircraft, naval, defense, and everything-combined — each compared against **the same group in the faction's own source game**, and each reported as an **independent separate value**. Geometric mean. |
| R6 | **Collect every stat we can get.** Some will turn out to be junk; that is a later triage, not a reason to narrow the extraction now. |
| R7 | **Variance is reported three ways**: coefficient of variation, min/max spread, and percentile position of the faction's mean within its own game. |
| R8 | ⚠ **AMENDED AT THE ENDS BY R19 (2026-09-21): beyond the outermost anchor the value is now EXTRAPOLATED, not held flat**, and both interpolation and extrapolation are geometric. The rest of R8 stands. **Armor extrapolation: interpolate between the peer's OWN declared rungs.** Every value a peer actually declares is a fixed anchor on its ladder; rungs between two anchors are interpolated; ~~beyond the outermost anchor the value is held flat~~ (see R19). A peer that declares only ONE rung on a ladder votes flat and is **downgraded to low confidence**. ⛔ Never distribute using Cameo's own §12.0i curve — that would make the "independent" reference partly a measurement of ourselves. |
| R9 | **Derived corpus is committed under `docs/reference/`; raw game data stays outside the repo** in `Cameo-mod-reference/`. |
| R10 | **Sequencing: the C&C family is built FIRST** — TD, RA1, TS, RA2 and the RA2-modded factions, from the references we have. Warcraft, StarCraft and Dune factions come later, each from **their own** reference pool (StarCraft mods, WC2 mods, Emperor: Battle for Dune, Spice Wars, the custom D2k mods). Measured and safe: of the **22 classes in use, ZERO are deferred-only** — every class a Warcraft/StarCraft/Dune unit sits in also has C&C members, and C&C members dominate all 16 mixed classes (`mbt` 33:8, `support` 30:4, `fire_support` 26:4). Deferred factions keep global-formula pricing meanwhile; waiting does not make them more wrong. ⛔ **The protection this depends on: anchor values are FROZEN design numbers. Re-fitting an anchor is an explicit maintainer act and must NEVER happen as a side effect of adding a reference source** — a re-fit moves every already-balanced faction underneath itself. ⚠ `archer` (2 C&C members) and `heavy_infantry` (1) are too thin to anchor on C&C evidence alone; leave them formula-only until their own references land. |
| R11 | **Cameo 1.0 release gate: EVERY faction must be written with the new balance formula, and EVERY faction must have reference data to choose from.** No faction ships 1.0 on formula-only pricing. The per-reference-faction calculated values are what populate each unit class relative to the baseline — so a faction with no reference has nothing to position against. This makes the deferred reference sources (Emperor, Spice Wars, StarCraft, WC2, the custom D2k mods) **release-blocking for 1.0**, not optional enrichment. |
| R12 | **DTA Enhanced represents DTA; Classic is a superseded lineage member.** They are one roster — Enhanced is Classic plus `Enhance.ini` — and measured 80–92% identical on the 863 shared ids with a median ratio of exactly 1.000. Enhanced is elected on the game's own default (`Items=Classic,Enhanced`, `DefaultIndex=1`). ⚠ Not a no-op: `td_gdi`, `td_nod`, `ra1_allies` and `ra1_soviets` move onto the overlay's ~10–20% rebalanced rows. |
| R13 | **Rise of the East is exclusive-only**, like Mental Omega and CnC Reloaded. Its 36% `asianalliance`/`tkm` overlap was inside the honest C&C band, so this is a cheap improvement rather than a rot fix: RotE has large per-country pools, so exclusivity costs ~4 rows a faction and takes the overlap to **0%**. |
| R14 | **The universal-pool carve-out.** A unit owned by EVERY routed country of its source is either shared infrastructure or a universal COMBAT unit; the mobile ones are readmitted, the structures are not. Found by asking what R13 actually removed: not harvesters — Rise of the East's Soviet Mammoth Tank, Asian Emperor Overlord Tank, Allied Juggernaut and Yuri Specter Squad, i.e. the top of the HP and cost range. ⚠ "Universal" means ALL routed countries; owned by SOME is the case the partition exists for and stays removed (Mental Omega's 61-unit non-Foehn pool; CnC Reloaded's Core Defender, 19 of 21 countries but not Nod). Overlap among the five CnCR-fed factions is 16–23%, against 81% before any exclusivity. |
| R15 | **A source's own NAMING beats a permissive `Owner=`, and a name claim is EXCLUSIVE.** CnC Reloaded ships a complete CABAL faction — 55 units named `CABAL's ...` plus the Core Defender pair, every one owned by `RobotCountry` — but gives 43 of them to all 21 countries, so ownership alone would discard the faction. A modder writes "CABAL's Refinery" on purpose and copies an `Owner=` line out of habit. A claimed unit goes to its claimant and to **nobody else**: CABAL's Cyborg Commando stops being a reference for `ra2_allies` and `ts_gdi`. `cabal` is routed to CnCR RobotCountry and goes **44 → 156 rows**, clearing the two-source floor it was the only routed faction to fail. ⛔ I had recorded that CnCR shipped no CABAL faction; that came from sampling DEVOUT and ASCENDED, seeing all-21 ownership, and generalising from two units. |
| R16 | **A cell an attack CANNOT REACH is `n/a`, never 100.** `WeaponInfo.ValidTargets` and `Warhead.ValidTargets` both default to `new("Ground","Water")` (WeaponInfo.cs:116, Warhead.cs:30), so a weapon that never mentions targeting cannot hit air at all — **366 of Combined Arms' 695 weapons**. Filling the unstated Aircraft row with the engine's 100 entered every tank cannon into the corpus as a competent anti-air weapon and polluted the whole AIR ladder: **74% of CA's air cells and 48% of Cameo's own**. `n/a` and "immune" (`Versus 0`) are both excluded from every mean and are **different statements**. |
| R17 | **Each source matrix is normalised to a usage-weighted GEOMETRIC mean of 100, inside Cameo's own 20:1 window.** §12.0 rule 4's `[10,200]` band is unit-free because it is relative to the matrix's own centre, which is what makes DTA (centre ~590) and OpenRA TD (~61) comparable. ⛔ **Zeros are ABSENCES: excluded from the centre and from the clamp.** Treating a zero as `centre × 0.10` makes the window a runaway — measured on Mental Omega (20.8% zeros) the centre fell 32.3 → 13.5 → 4.1 → 0.2 and never settled, flattening the matrix. Every source above ~13% zeros did it; below ~8% it converged, which is why the flaw stayed invisible until the YR mods were added. |
| R18 | ⚠ **"Geometric mean is 100 and the range is [10,200]" IS NOT A CHECK.** It holds *by construction*, because the matrix is normalised by the very centre being tested — it passed on all six degenerate sources. The independent quantity is the source's own **positive median**: a matrix has collapsed when its typical working value no longer fits inside its own window. `warhead_matrix.py --check`. |
| R19 | **Amends R8 at the ENDS.** R8 holds a value flat beyond the outermost anchor; maintainer 2026-09-21 ruled the opposite for the ladder ends — *"Armor values in between must be interpolated and scout and superheavy must be extrapolated"*. Interpolation and extrapolation are both **geometric** (linear in log space): these are multipliers, so the midpoint of 50 and 200 is 100, not 125. R8's ban on redistributing through Cameo's own §12.0i curve is untouched and still binding. |
| R20 | **One mod, one vote — and Cameo votes too.** Combined Arms (759 warheads) and OpenRA TD (57) each count once; usage weighting decides a mod's INTERNAL centre, never how loudly it speaks. Cameo's vote is cast by its **147 `^Warhead_*` templates**, not the resolved weapons, whose un-retrofitted legacy tail would be a vote for the status quo. This extends R4's equal-thirds principle to the armour layer. |
| R21 | **A reference warhead's family is assigned BY HAND from the compression groups, never by matching its name.** Name matching failed three times in one session and the last one is the clearest: `BazAP` was classified as Tesla because `Ba`**`zAP`** contains "zap"; `ChronoBeam` and `LocomotorBeam` matched "Laser" and are mind-control effects with no damage profile. Delivery comes from `Projectile:`, element from `DamageTypes:`, platform from the actors that fire it — all measured. ⚠ Most `*Death` tokens are DEATH ANIMATIONS, not elements: CA's `HonestJohn` rocket artillery carries `FireDeath`, so Fire requires the real `Incendiary` tag. |
| R22 | **One warhead per weapon on the reference side too.** 116 of Combined Arms' 466 armed weapons carry more than one damage warhead; entering each as a separate weapon put one weapon in three groups. Mirrors DESIGN.md §11b for our own side. ⛔ **AMENDED by R23** — the half of this ruling that said *how* to reduce a weapon to one row is struck: ~~`Warhead@1Dam` is the main in 550 of 577; where the convention is absent the largest `Damage` decides~~. That count came from a scan that treated every warhead node as a weapon, and picking by name reported false immunities. Fold, do not pick. |
| R23 | **A weapon's profile is the SUM of its warheads, not its biggest one.** The maintainer queried four groups in the 2026-09-21 review — *"why is light immune?"*, *"how is the damage so low against None?"*, *"none immune is a bug"*, *"anti light only at 18 while the rest is all at 176?"* — and all four were one measurement defect. OpenRA fires **every** warhead on every hit, and CA's extra warheads are routinely COMPLEMENTARY: `FireballLauncher` states `Light: 0` on `@1Dam` and `Light: 50` on `@2Dam`; `JDAM` and `MaverickSU` put their whole anti-infantry half on `@2Dam`; `ApocRadBeamWeapon`'s `@1Dam` is an infantry-only `HealthPercentageDamage` rider. So fold by effective damage — `versus(A) = 100 x SUM(damage_w x versus_w(A)) / D`, `D = SUM(damage_w)`, a warhead that cannot reach `A` contributing 0 to the numerator and still counting in `D`, which keeps the row RATIOS equal to real effective-damage ratios. ⚠ A per-armour denominator (a plain weighted average) flattens exactly the weapons that split their target sets. ⚠ `HealthPercentageDamage` is a different currency (`HP x Damage/100 x Versus/100`) and folds only with its own kind. Implemented as `warhead_matrix.fold_by_weapon`. |
| R24 | **A percentage warhead is priced through the typical unit it hits.** Maintainer: *"percentage warheads need to be regarded as a flat damage against the average unit of that armor type, so 300% against the average of 50k HP light armor means it's 150k damage against light"*. `HealthPercentageDamage` resolves as `HP x Damage/100 x Versus/100`, so its damage against class `A` is `avgHP(A) x Damage/100` — armour-DEPENDENT, which is exactly the property that makes a percentage weapon relatively better against heavy classes. `avgHP` is the GEOMETRIC mean HP of the buildable actors declaring that armour (CA: None 6,336 · Light 18,774 · Aircraft 23,524 · Concrete 48,973 · Heavy 52,994 · Wood 89,166). Excluding percentage warheads instead — the first attempt — made a sniper whose entire anti-infantry punch is a 300%-of-HP warhead read as a mild anti-vehicle gun. ⚠ Overkill is deliberately NOT modelled, on the maintainer's instruction; the flat side is not capped either, so capping one would bias the comparison rather than fix it. |
| R25 | **Reachability and coverage are resolved per ARMOUR CLASS, not per GND/AIR macro.** R16's macro mask is enough to decide `n/a`, and NOT enough to fold: 380mm's second warhead deals 70,000 with `ValidTargets: Infantry`, and summing that into Heavy and Wood — both "ground" — turns a siege gun into a tank killer. So a warhead reaches armour `A` only if some buildable actor declaring `A` carries a matching `Targetable` type, and it contributes in proportion to the FRACTION of that class it can hit. Coverage is not optional: 3 of Combined Arms' 63 Light-armour actors are cyborgs declaring `Infantry`, so without it a sniper's infantry-only rider reached the Light column at full strength and put `Light` ABOVE `None` on a sniper. |
| R26 | **The reference map is ONE ROW PER CAMEO WARHEAD.** Maintainer: *"everything that shares the same cameo warhead type is only listed once in one list ... one group per cameo warhead mapping"*. `compress_warheads.py` answers what distinct behaviours a mod SHIPS (126 groups for CA); `family_matrix.py` answers what each of OUR warheads must BE (32 live families), collapsing the groups onto their assigned family by usage-weighted geometric mean. A per-weapon override beats its group, so a group contributes only the weapons that stayed with it and its usage is split across its destinations. |
| R27 | **An AA armament is not a separate weapon — fold the pair at ACTOR level.** Cameo's shipped dual-armament law is *"AA split = one weapon"*, the air side earning a 1.5x range bonus and anti-air vehicles +100% firepower — a MODIFIER layer, not a second warhead. Combined Arms splits them, and counting the halves separately invented a family: `MissileAA` was the third-largest in the map at 43 weapons, of which **39 were the AA half of a dual-purpose unit** (Flak: 7 of 9; Laser: 5 of 6). ⚠ Only an UNAMBIGUOUS pair folds — exactly one ground and one air armament on the actor. The Venom's pairing is obvious; the IFV carries thirty ground weapons and four AA ones and choosing a partner there would be a guess. The merged magnitude is the USAGE-WEIGHTED GEOMETRIC MEAN of the two, never a sum (they never fire at the same target), and the pair takes its GROUND half's family. Result: MissileAA 33 air-only weapons, Flak 3, and every dual-purpose ground weapon stops reporting `n/a` against aircraft it can plainly shoot down. |
| R28 | **No `LaserAA` and no `LaserAir`.** Both were considered and rejected on measurement. A dual-purpose laser is already expressed by the R27 modifier layer, so encoding it in the Versus table as well would double-count it; and a genuinely AA-ONLY laser has one candidate in the whole corpus (`MantisLaser`; in Cameo, the Cabal Obelisk of Darkness), which does not earn a warhead. ⭐ Generalises: a dual-purpose family per delivery is minted only where a reference mod shows a real, distinct profile — never for symmetry. |
| R29 | **A family's profile pools its member WEAPONS' rows, never their groups' profiles.** A group is a REVIEW device (120 rows a human can read instead of 550), and a per-weapon override routinely sends one member somewhere else. Pooling group shapes drags the whole group along with that one weapon: `MissileAA` came out with a full ground profile (Heavy 105.6, Light 182.7) on the strength of ONE of its 33 members sitting in a ground-capable group, when every other member is air-only and `n/a` there. |
| R30 | **Cameo votes as one mod among the twenty, and uniqueness is resolved only after all twenty are in.** Extends R20 from the armour layer to the warhead layer. Separation applies on COLLISION only, along the axis where the sources disagree most — so today's 8 near-identical CA pairs (`Laser`/`Tesla` 0.084, `Flame`/`Plasma` 0.088, `Bullet`/`Arrow` 0.105 …) are recorded, not acted on: CA is one vote of twenty and a collision on its data alone may not survive averaging. |
| R31 | **NO CAMEO WARHEAD IS AIR-ONLY — the landed-aircraft rule.** Maintainer: *"MissileAA should still be able to damage ground targets but have the highest versus value against air. What if the aircraft lands? Then it counts as ground, and when the missile is still in flight and the aircraft lands it should of course still deal damage."* The MEASUREMENT stays honest — CA's SAM sites really do state no ground opinion, and writing 100 there is the Aircraft-148 defect pointed the other way — but a Cameo warhead has to fire at whatever it is aimed at. So an air-only family's ground rows are DERIVED from the same DELIVERY's measured warheads (a SAM is a missile) and scaled by `AA_GROUND_RATIO` (0.5) of the measured air value, so the air row stays the peak. Those cells are marked `derived_ground` and shown differently on the map. ⚠ A FALLBACK, not a law: most reference mods DO give their flak cannons and SAM sites real ground rows, so these cells should be replaced by measurement once the other nineteen sources are averaged in. |
| R32 | **Pair armaments by DISTINCT PROFILE, not by distinct name.** OpenRA gives one logical weapon several armament entries for mechanical reasons — firing ports, left/right mounts, garrison slots. The Yak carries `ChainGun.Yak.L` and `ChainGun.Yak.R`, byte-identical profiles on two wing mounts, and counting them as two ground weapons made the actor look ambiguous and blocked the R27 fold of its obvious `ChainGun.Yak.AA`. Counting distinct profiles took Combined Arms from 17 folded pairs to 29. ⚠ And one ground weapon can adopt SEVERAL air variants (`HellfireAG` pairs with both `HellfireAA` and `HellfireAA.Cryo`): merge once per GROUND weapon, or the result carries two rows with the same weapon key and every consumer keyed by weapon name silently collapses them. |
| R33 | **A source's armours are ANCHORED to positions on our ladders, then LOG-LINEARLY interpolated.** Implemented as `tools/reference/armor_interpolate.py`. An anchor pins a source armour at an index on one of our four ladders (INF 3 rungs, VEH 5, BLD 3, AIR 4) — exactly (`combined_arms.None` IS our `None`), fractionally (`combined_arms.Light` at VEH 0.5, between Scout and Light), flat across a whole ladder (`combined_arms.Aircraft`, the only source with a real air tag), or excluded (walls, terrain, projectile-interception classes). Unpinned rungs are filled in LOG space, because these are multipliers and the step between rungs is a ratio. ⚠ A ladder with ONE anchor fills only its own rung: a single point carries no slope, and inventing the rest would invent the source's opinion — OpenRA Red Alert genuinely has no second infantry rung and so has no view on Flak. ⚠ EXTRAPOLATION IS CLAMPED to the §12.0 window [10, 200]: the transcribed Dune 2000 table pins its building ladder only at Steel and Concrete, and running that slope down to Wood produced 250. ⭐ `Heroic` is never interpolated — §12.0b makes it the derived column `Plate x Scout / peak`, recomputed after the ladders are filled. |
| R34 | **SHAPE and LEVEL are averaged separately, because the sources do not all vote on every rung.** A plain per-cell mean compares populations of different size and invents an inversion no source contains: `None` had 18 voters and `Flak` only 9, the nine extra `None`-only voters were the low sources (Tiberian Sun 81, Tiberian Dawn 89, Dune 2000 91), and the average came out `None` 109.4 BELOW `Flak` 119.9 — while **8 of the 9 sources with an opinion on both rungs put them the right way round**. §12.0d says a ladder can never invert, so that was a broken measurement, not a finding. ⭐ A source that pinned TWO OR MORE rungs of a ladder votes on that ladder's SHAPE (its own rungs normalised to a geometric mean of 1, which makes sources with different coverage comparable); every source that pinned ANY rung votes on the ladder's LEVEL; the answer is shape x level. Nobody's data is discarded and nobody's absence distorts a rung. All four ladders then come out monotonic from 18 independent sources: INF 112/111/98, VEH 134/117/100/88/78, BLD 106/86/67, AIR 122/116/109/103. |
| R35 | **RESOLVED — a source's vote on a ladder is weighted by how much of that ladder it really has.** An armour that anchors two ladders contributes ONE measured number to both, and that number is dominated by whichever macro type wears the tag most. Combined Arms is the worked example: its `Heavy` anchors our Plate infantry rung AND the top of the vehicle ladder, but of its **101 wearers, 98 are vehicles and 3 are cyborgs** (ENLI/REAP/RMBC — genuinely CA's heaviest infantry, but targeted as `cyborg`+`vehicle`, never `infantry`), so Plate was being set by a number describing heavy tanks. CA was the only source of eighteen to invert its own infantry ladder. ⭐ Fix: weight each source's SHAPE vote on a ladder by the geometric mean of its anchors' wearer share for that ladder's macro type. Nothing is discarded — CA still speaks about infantry, just quietly. The infantry ladder went from 112.1/110.5/98.3 to **112.8/108.7/99.3**, and all four ladders are monotonic. A source with no actor census (the INI dialects, the transcribed table) speaks at full strength: its armour names are already per-macro-type. |
| R36 | **A cyborg is infantry x vehicle and votes HALF on each ladder** (maintainer's ruling, 2026-09-21). DESIGN.md's *Cyborg dual-armor rule* already says why: a CABAL cyborg keeps its infantry-class `Armor` and adds a vehicle `Armor@<role>`, *"so cyborgs count as both infantry and vehicles for weapon Versus tables"*, and the two rows are **AVERAGED, not multiplied** (`AreaDamageWarhead.MultiArmorCombination: Average` — `Plate` 88 with `Superheavy` 10 resolves to 49, not 8). The same pattern covers Future Tech droids and Schwarzer Mond. So in the wearer census a cyborg counts 0.5 toward INF and 0.5 toward VEH, never 1.0 to either. ⚠ This is also why Crystallized Nexus looked like it had an overlapping `Vehicle` tag: it runs the same two-layer scheme, a macro tag multiplied with a weight class. |
| R37 | **Cameo's own vote needs no compression — its template NAME is the family.** `compress_warheads` looked the source id up in `OPENRA_SOURCES`, which lists the REFERENCE mods only, so Cameo's groups came out delivery `Unknown` / band `unused` while R20 and R30 both give it a vote. `cameo_resolved` is fixed by adding this repository's own ruleset to the lookup (174 groups, 173 with a real delivery). The TEMPLATE view needs no fix at all: a `^Warhead_*` template is abstract and has no `Projectile:`, but `^Warhead_Arrow_Heavy` IS the Arrow family by definition. ⚠ That is not a breach of R21's ban on name-matching: R21 forbids inferring a REFERENCE mod's weapon class from its name, where the names lie; these are our own names, which are the vocabulary itself. All 150 templates parse into **50 families** x {Light, Medium, Heavy, Super}, against the 32 Combined Arms needs — the gap is the consolidation target. |
| R38 | **RESOLVED — the Westwood INI dialects carry delivery and element in a different shape, and it is extractable.** `compress_warheads` keys on `Projectile:` and `DamageTypes:`; the INI mods have neither, and the obvious substitute is a trap — Mental Omega's 1,117 weapons name projectiles `InvisibleWork`, `InvisibleHigh`, `CannonInviso`, which are trajectory and rendering variants, not weapon classes. Nine sources compressed to nothing but `Unknown`. ⭐ The real signals sit in three places and every token was COUNTED in the files rather than taken from documentation: **weapon flags** `IsLaser` (MO 145, TI 18), `IsBigLaser`, `IsElectricBolt` (MO 42), `IsRailgun` (TI 25), `IsDetachedRailgun` (MO 93), `IsRadBeam`, `Lobber`; **the [Projectile] section's behaviour** `ROT` (it steers, so it is a missile), `Arcing` (ballistic shell), `Inviso` (MO 121, hits instantly), `Vertical` (a bomb); **warhead flags** `Fire`, `Tiberium` (MO 48, TI 50), `EMEffect`, `Bullets` (MO 71), `Radiation`, `Sonic`, `Temporal`, plus `InfDeath`. ⚠ `InfDeath` IS A DEATH ANIMATION, the same trap as OpenRA's `*Death` tokens, so only the codes unambiguous across TS and RA2 are used — 3 burn, 4 electrocute, 6/7 chemical — and explicit flags always win. The band comes from which registry (`InfantryTypes`/`VehicleTypes`/`AircraftTypes`/`BuildingTypes`) the firing actor is listed in. Result: **17 of 20 sources compress, 1,202 groups across the corpus** (Mental Omega 17 -> 130, Rise of the East 13 -> 105, Red Resurrection 14 -> 104, DTA 9 -> 35). The three that do not are by construction: `cameo` needs no compression (R37), `cameo_resolved` is at 173 of 174, and `d2k_mod` is a transcribed table with no actors, so it votes on the armour ladder only. |
| R39 | **MEASURED, AND IT FAILS — family propagation by the delivery/element/band triple is 19% accurate, and the GROUP, not the method, is why.** R38 left 512 of 1,059 groups needing a ruling and the other 547 as inherited proposals; nothing had checked whether an inherited proposal was ever right. It is checkable for free: a Cameo weapon inheriting `^Warhead_<Family>_<Level>` states its family in the yaml (R37), and **904 concrete weapons do**, so compressing Cameo's own weapons and asking the propagation to recover Cameo's own answers is a labelled test that needed no maintainer time. Result on the 53 groups with >=3 labelled members: **propagation by triple 19% top-1** (the right family is somewhere in its top-4 candidates 38% of the time), and **nearest-profile-shape is WORSE at 11%** (top-3: 19%). ⚠ But the ceiling is not 100%. At the shipped `tau = 0.50` one group holds a **median of 3 distinct Cameo families**, worst 15, and only **11 of 53 groups are 80% one family** — no single-family label can be right about `BulletHE_Veh`, which is 296 uses spanning CannonHE, Bullet and Demolition. **~61% is what any method is actually playing against.** ⭐ The fix is the clustering threshold, not a cleverer matcher: purity is a clean function of `tau` — 0.50 -> 61% (162 groups), 0.30 -> 67% (241), 0.20 -> 75% (302), **0.10 -> 89% (413)**. ⚠ And usage weighting makes it WORSE, not better (53% at tau 0.50, below the unweighted 61%), because the big groups are the mixed ones: triaging to "just the important groups" reviews precisely the ones a single label fits worst. Harness: `tools/reference/validate_families.py` (`--purity` for the sweep, `--shape` for the alternative). ⛔ **BLOCKS the plan of reviewing 1,059 tau-0.50 groups** — that review would have been recording labels that are wrong about a third to a half of each group's members, and no later averaging recovers from it. |
| R40 | **RULED (tau 0.20 + drive outward) — and the outward fit by SHAPE is a measured dead end, which relocates the whole task.** Maintainer, 2026-09-22: *"a mix of the tau 0.20 and the drive cameo's families outward ... I don't actually know some of those reference mods like red resurrection and ra2 reborn so I can't give my opinion on those, so you should try to first fit everything from all mods into that reference table yourself."* Groups therefore re-cluster at **tau 0.20**, which R39 measured at 75% purity, and the migration is cheap because the clustering is agglomerative and NESTS: of Combined Arms' 118 reviewed groups **76 do not split at all** and carry forward verbatim, 41 split and 4 straddle — ~45 re-reads, not 118. All sources: 1,059 groups at tau 0.50 → **1,712 at tau 0.20** (1,991 at 0.10). ⛔ But matching groups outward to Cameo families BY VERSUS SHAPE scores **3%**, against the triple's 19%, and adding a delivery-compatibility penalty only reaches 4-6%. The cause is structural, measured two ways. **(a) Cameo's family shapes do not separate**: across the 150 live templates 27 PAIRS sit within 0.05 RMS log distance and `Magic`/`Sonic` centroids are IDENTICAL (0.000), so nearest-neighbour among 51 families is a coin flip inside a cluster. **(b) The target table is already made of the thing being matched to it** — Cameo's shipped `Versus` comes from `docs/reference/family_profiles.json` via `gen_weapon_template.py`, so the match is CIRCULAR where a family was measured and meaningless where it was not: the generator's fallback is `table()`, a LINEAR RAMP. ⭐ **Of the 51 live families, 10 are reference-derived, 8 are hand-designed (`invented_family_profiles.json`) and 33 — nearly two thirds — are that ramp.** The identical `Magic`/`Sonic` pair IS two ramps at one level. ⭐ **THIS RELOCATES THE TASK.** Finding (b) is the deliverable, not an obstacle: the pipeline exists to replace those 33 ramps with measured profiles, so the unit of work is the **CAMEO FAMILY (51 rows)**, not the 1,712 reference groups — routing family → groups by categorical identity (a `MissileCryo` IS delivery Missile, element Cryo) rather than group → family by resemblance. The existing 10 cleared `min_rows: 8, min_mods: 3` from an older single-machine extractor (`propose_family_profiles.py` over `survey_platforms.py`, which reads `~/Downloads`); the current pipeline carries 20 sources, so that gate should now clear far wider. Harness: `tools/reference/fit_families.py --validate` (kept as evidence — ⛔ its `--all` output is NOT an assignment). |

---

## §1 — What we are extracting from

**Two families of source, and they need different extractors.**

**A. INI mods** (Ares/Westwood engines) — already extracted, in
`Cameo-mod-reference/extraction/`, 8183 unit rows:

| source | units | costed | armor rows |
|---|--:|--:|--:|
| Rise of the East 3.0.0c | 2445 | 1666 | — |
| RA2 0XX 1.0.8 | 2104 | 486 | 684 |
| Mental Omega 3.3.6 | 1706 | 786 | — |
| CnC Reloaded 2.7.0 | 1306 | 816 | 355 |
| Red Resurrection 2213 | 1048 | 491 | 480 |
| DTA (Classic + Enhanced overlay) | 869 + 112 | 417 + 85 | `Modifier.*` |
| RA2 Reborn 1.0.31 | 697 | 366 | 176 |

**B. OpenRA peer mods** — 16 declared in `tools/reference/extract_peer_units.py:PEERS`:
`ca` (Combined Arms), `cn` (Crystallized Nexus), `sp` (Shattered Paradise), `rv` (Romanov's
Vengeance), `hv` (Hard Vacuum / OpenHV), `e2140` (OpenE2140), `gen` (Generals Alpha), `fnw`,
`ra2vsh` (Valiant Shades), plus the upstream `ra`, `ra2`, `yr`, `ts`, `cnc`, `d2`, `d2k`.

✅ **A1/A2 DONE 2026-09-05** — `extract_peer_units.py` now runs. **9 of 16 peers resolve, 1946 units**:
Combined Arms 382, Romanov's Vengeance 729, Shattered Paradise 306, Generals Alpha 153,
Crystallized Nexus 97, OpenRA TD 56, RA 94, TS 73, D2k 56 — the last four from the
`cameo-engine` clone, which already ships `mods/{cnc,ra,ts,d2k}`.

⏳ Still missing 7, ranked by the armor map's own assessment: **Valiant Shades** (AS-lineage,
highest confidence, tied with RV), **OpenHV**, **OpenE2140**, OpenRA RA2, Yuri's Revenge on
OpenRA, Fractured Realms, Dune II (declares NO `Versus` — unit stats only). Non-blocking.

---

## §2 — The build order

### Phase A — extraction

| # | task | notes |
|---|---|---|
| **A1** | ✅ DONE — **Port `mod_id` into `tools/audit/miniyaml.py`** | ⛔ BLOCKER. `Ruleset.__init__` takes one arg; `extract_peer_units.py` calls it with two. The change exists on `origin/claude/docs-audit-reorganize-xgzwhr` (27 insertions, `mod_id: str = "cameo"` default, so every existing caller is unaffected). Devin landed the tooling without it. |
| **A2** | ✅ DONE — Resolve all 16 peers on disk; report which are missing | `--dry-run`. A missing root is skipped near-silently — read every line. |
| **A3** | ✅ DONE — INI extractor → the same schema as the OpenRA extractor | RA2/YR 11-slot `Verses=`, TS 5-slot, DTA `Modifier.*`. |
| **A4** | ✅ DONE — Every stat, both families (R6) | core (HP/cost/speed/range/DPS/armor) + build/economy + combat detail + vision/utility. |
| **A5** | ✅ DONE — Every `Versus` / `Verses` / `Modifier.*` row | ⚠ In OpenRA yaml `Versus` is a **node with an EMPTY value whose children are the rows**. `node.get("Versus")` returns empty and yields the false result "0 peers expose Versus". Use `weapon_efficiency.versus_of`. |
| **A6** | ✅ DONE — Armor normalisation per R8 | onto the four ladders in `docs/reference/peer_armor_map.yaml`; confidence gates voting; only `high`/`medium` vote. |
| **A7** | ✅ DONE — Convert the two UTF-16 reference files to UTF-8 | `PEER_ARMOR_VOCABULARIES.md`, `peer_armor_map.yaml` — PowerShell `>` wrote them; every grep silently under-reads them. |

### Phase B — statistics (R5, R7)

| # | task |
|---|---|
| **B1** | ✅ DONE — Per-source, per-faction rosters, split by type: infantry / vehicle / aircraft / naval / defense |
| **B2** | ✅ DONE — Faction "average actor": geometric mean per type **and** overall |
| **B3** | ✅ DONE — The same aggregate for the **whole source game**, per type — the comparison group |
| **B4** | ✅ DONE — Faction profile = B2 ÷ B3, per type and overall, each an independent value |
| **B5** | ✅ DONE — Variance: CV, min/max spread, percentile position — faction vs its own game |

### Phase C — the mapping matrix

| # | task |
|---|---|
| **C1** | ✅ DONE — For each of the 29 real Cameo factions, assign its reference combination (R3, R4) |
| **C2** | ✅ DONE — Record it as reviewable data next to `faction_routes.py`, with the reason per pairing |
| **C3** | ✅ DONE — Verify every Cameo faction reaches the ≥2-reference floor, or is explicitly `UNROUTED` (formula-only) |

### Phase A/B/C — CLOSED 2026-09-06 by wiring the corpus into the distribution layer

`ini_corpus.json` existed from 2026-09-05 but nothing READ it: `reference_distribution.peer_rows()`
parsed two markdown documents only, so `faction_routes.py --check` reported all fifteen INI routes
as *"source not in the de-duplicated corpus"* and `allows()` admitted zero rows for them. The
loader is `reference_distribution.ini_rows()`. Corpus **2,568 -> 4,523 peer rows, 15 -> 21 sources**;
Cameo actors with a reference signature **324 -> 374**.

Four things that had to be settled to land it, each measured rather than assumed:

| what | finding |
|---|---|
| **DOC1 vs the extraction** | `ORIGINAL_UNITS_RAW.md`'s hand-typed Mental Omega / CnC Reloaded tables are the same data, less of it, with typos: median HP ratio 1.000 but MO's Lionheart Bomber reads 10,000 HP against `[LIONH] Strength=800`. DOC1 now stands down per source, automatically, for anything the extractor covers — it currently supplies **zero** rows. The one signature lost (`terran_ghost`) matched an MO row for a unit MO does not have. |
| **`cost > 0` is not a buildability test** | these mods price internal dummies at 1 credit. CnC Reloaded's `TSCARRYALL_DUMMY` is a costed **10,000,000 HP** row against a real ceiling of 6,000. `TechLevel = -1` + `Selectable/IsSelectableCombatant = no` are the engine's own flags and fix every tail (RotE 15,000 -> 2,000, RA2 0XX 9,999 -> 3,000). They also drop the elite/upgraded DUPLICATE actors these mods ship, which were double-counting their own base unit. |
| **route tokens were never case-folded** | `peer_factions()` lowercases the corpus; the INI routes are written in each source's own casing. All fifteen matched nothing while `--check` printed the token it wanted next to the same token, lowercased, in the same line. |
| **"exclusive" means different things per source** | CnC Reloaded has real per-faction pools (nod 86, gdi 61, soviet 42, allies 41, yuri 32). **Mental Omega has almost none — 3 to 7 per country** — because it models four SIDES; country exclusivity cut `japan` to 6 units. Exclusivity is now a declared PARTITION per source, and `faction_routes` and `faction_profile` share one copy of it. Worst Cameo-faction roster overlap **97% -> 49%**. |

### Phase D — virtual anchors (R1, R2)

| # | task |
|---|---|
| **D1** | Migrate `docs/balance/class_anchors.json`: drop `anchor_actor` / `verifier_actor`, keep `spec` as the definition at 100% of the band |
| **D2** | Write the resolver-check audit: resolve **every** unit in a class, assert the formula reproduces its price |
| **D3** | Update `FORMULA_V2.md` §2 — it currently rules that every class has a *living baseline unit in game*, which R1 replaces |

---

## §3 — Traps already paid for. Do not rediscover these.

1. **`git grep` skips several of our weapons yaml as binary** (non-UTF-8 bytes), and **`miniyaml`
   silently under-parses the same files.** For any presence check use
   `git show <rev>:<file> | grep -a`. This nearly deleted 30 live weapon nodes.
2. **A mod's loose `rulesmd.ini` can be vanilla Yuri's Revenge byte-for-byte** — Mental Omega's is
   (md5 `cf7eb658327aff1fe7e6c4e7400eb87f`). Check every extraction against that hash.
3. **`extract_mix_ini.py` sniffs only the first 4096 bytes**, so a rules file with a comment banner
   reports "0 INI blobs found". Judge blobs by full content.
4. **`Versus` is an empty-valued node** — see A5.
5. **A claim about the corpus is not a claim about the world.** "MO and CnC Reloaded are not
   recoverable" was recorded as fact in `REFERENCE_PIPELINE_HANDOFF.md` §1.3; both were on disk.
