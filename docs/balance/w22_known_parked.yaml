# W22 known-parked actors — `~disabled` convention.
#
# Maintainer ruling (fleet orders, 2026-09-26): the actors below are
# intentionally parked, NOT dead content. Do not delete them without an
# explicit maintainer ruling. `w22_roster_census.py` cross-checks this list
# and warns on drift; `w22_triage_unreachable.py` classes them `parked`.
#
# Entries annotated `spawn_only` stay in this manifest for the record but
# are census-classed `spawn_only` — they are produced at runtime (SpawnActor
# warheads / spawn traits) despite the `~disabled` prereq.
- ra1_allies_cybertank
- ra1_soviets_kennel      # also placed by bundled maps (map_script_only in triage)
- rapt
- tsldrone
- tsaegis
- cabal_artilleryspider_backup
- cabal_tarantula_backup
- forgotten_chinook
- forgotten_scarabapc     # spawn_only — produced by mutsapc
- ra2_c_hum2
- ra2_c_abram
- steel_cougar            # spawn_only — steelsupportspawner*
- steel_hummer            # spawn_only — steelsupportspawner*
- steel_oldqtnk           # spawn_only — steelsupportspawner*
- steel_cobra             # spawn_only — steelsupportspawner*
- naxis_conehead2
- naxis_assault
- tkmdrone                # spawn_only — tkmdronesummon warhead
- tkmvan
- siege_tank
- wc2_orc_skeleton        # spawn_only — SpawnActor warhead (Raise Dead)
- wc2_orc_eye_of_kilrogg  # spawn_only — SpawnActor warhead (Eye of Kilrogg)
- wc2_neutral_daemon      # spawn_only — summon, no live producer wired yet
