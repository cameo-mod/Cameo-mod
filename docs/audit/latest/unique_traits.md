# audit_unique_traits — traits that must be unique per actor

_Scanned 3119 actors against 11 trait types resolved with `.Trait<T>()`._

_clean_ — no actor declares two nodes of a trait the engine resolves uniquely.

