# ai.yaml — CNC faction AI configuration
#
# ARCHITECTURE NOTE
# =================
# All AI bot modules (BaseBuilderBotModuleCA, UnitBuilderBotModuleCA,
# SquadManagerBotModuleCA, SupportPowerBotASModule, etc.) are defined
# as single trait instances on the Player: actor in the global
# mods/cameo/ai/ai.yaml. Their sub-sections (BuildingLimits,
# BuildingFractions, UnitsToBuild, UnitLimits) contain ALL faction
# data in single dictionaries that CANNOT be split across files —
# OpenRA's YAML merge replaces trait instances with the same @name,
# it does not deep-merge their sub-sections.
#
# This file is loaded as a Rules: entry but contains no traits —
# it is a placeholder ready for future per-faction bot module
# splitting (see ROADMAP Phase E backlog).
#
# REFERENCE DATA (from global ai.yaml)
# ====================================
#
# --- BuildingLimits (7 entries for td_gdi) ---
# 			td_gdi_tiberiumrefinery: 10
# 			td_gdi_barracks: 10
# 			td_gdi_weaponsfactory: 10
# 			td_gdi_helipad: 10
# 			td_gdi_communicationscenter: 1
# 			td_gdi_advancedcommunicationscenter: 2
# 			td_gdi_repairfacility: 1
#
# --- BuildingFractions (12 entries for td_gdi) ---
# 			td_gdi_constructionyard: 5
# 			td_gdi_tiberiumrefinery: 15
# 			td_gdi_barracks: 15
# 			td_gdi_weaponsfactory: 15
# 			td_gdi_helipad: 15
# 			td_gdi_communicationscenter: 1
# 			td_gdi_advancedcommunicationscenter: 5
# 			td_gdi_ioncannonuplink: 1
# 			td_gdi_repairfacility: 1
# 			td_gdi_guardtower: 20
# 			td_gdi_skyshield: 10
# 			td_gdi_advancedguardtower: 20
#
# --- UnitLimits (2 entries for td_gdi) ---
# 			td_gdi_tiberiumharvester: 20
# 			td_gdi_chinooktransport: 5

# Player: actor — no traits defined yet.
# When the engine supports per-faction bot conditions, move the
# faction-specific BuildingLimits/BuildingFractions/UnitsToBuild
# entries here as a BaseBuilderBotModuleCA@<faction> /
# UnitBuilderBotModuleCA@<faction> trait with RequiresCondition.
Player:

