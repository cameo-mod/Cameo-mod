# Named playlists for the Cameo mod jukebox.
# Each top-level key is the playlist ID (used internally).
# The value after the key name is the display name shown in the UI.
# Tracks: lists track keys exactly as they appear in music.yaml.
# Tracks that are not installed or are marked Hidden are silently skipped.
